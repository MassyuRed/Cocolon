name: Cocolon system context inventory, code index, and route graph
run-name: Cocolon system context A fresh @ ${{ github.sha }}

on:
  push:
    branches:
      - agent/cocolon-system-context-index-20260818
    paths:
      - tools/cocolon_context_inventory.py
      - tools/cocolon_context_code_index.py
      - tools/cocolon_context_routes.py
      - tools/cocolon_context_routes_*.py
      - tools/cocolon_context_ts_syntax.cjs
      - tools/cocolon_context_ts_routes.cjs
      - tests/cocolon_context/**
      - Cocolon_前提資料/system_context/workspace_profiles.json
      - Cocolon_前提資料/system_context/Cocolon_SystemContext_ImplementationContract_20260818.md
      - .github/workflows/cocolon-system-context-inventory.yml
      - .github/context-payloads/step2-code/**
      - .github/context-payloads/step3/**
      - .github/context-payloads/step3-ts-routes/**
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: cocolon-system-context-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - name: Checkout Cocolon source branch
        id: checkout_cocolon
        uses: actions/checkout@v4
        with:
          ref: agent/cocolon-system-context-index-20260818
          fetch-depth: 0

      - name: Checkout locked mashos-api CMEE workspace
        id: checkout_mashos
        uses: actions/checkout@v4
        with:
          repository: MassyuRed/mashos-api
          ref: agent/cmee-v1a-i1sx-source-explicit-20260815
          path: .cocolon-context-workspace/mashos-api
          fetch-depth: 0

      - name: Clear prior one-shot workflow failure marker
        id: clear_failure
        shell: bash
        run: |
          set -euo pipefail
          rm -f Cocolon_前提資料/system_context/current/cmee_working/workflow_last_failure.json

      - name: Verify admitted Step 3 payload lineage
        id: materialize
        shell: bash
        run: |
          set -euo pipefail
          PAYLOAD_DIR=.github/context-payloads/step3
          ADMITTED_DIR=/tmp/cocolon-step3-admitted
          rm -rf "$ADMITTED_DIR"
          mkdir -p "$ADMITTED_DIR"
          cat "${PAYLOAD_DIR}"/part* > /tmp/cocolon-step3-payload.b64
          echo "d7893436ace9d1e7f8791b327473e3c11810c09bd72152f610934217267717dc  /tmp/cocolon-step3-payload.b64" | sha256sum -c -
          base64 -d /tmp/cocolon-step3-payload.b64 > /tmp/cocolon-step3-payload.tar.gz
          ACTUAL_LIST="$(tar -tzf /tmp/cocolon-step3-payload.tar.gz | LC_ALL=C sort)"
          EXPECTED_LIST="$(printf '%s\n' \
            tests/cocolon_context/test_code_index.py \
            tests/cocolon_context/test_routes.py \
            tools/cocolon_context_routes.py | LC_ALL=C sort)"
          test "$ACTUAL_LIST" = "$EXPECTED_LIST"
          tar -xzf /tmp/cocolon-step3-payload.tar.gz -C "$ADMITTED_DIR"
          echo "847303f952dd9fd6a7fd9540c5a42cf53751375cb35c86c863fc78d3bee4c3fe  ${ADMITTED_DIR}/tools/cocolon_context_routes.py" | sha256sum -c -
          echo "4d520138b2c92646bf97923a473a6548a2f108ac5f41076a9c7bd50c665aa60c  ${ADMITTED_DIR}/tests/cocolon_context/test_code_index.py" | sha256sum -c -
          echo "9a6f4d5d60ba7d17db5d4df10e27e84e9a1b7f253cb7555924327646364d02a8  ${ADMITTED_DIR}/tests/cocolon_context/test_routes.py" | sha256sum -c -
          test -f tools/cocolon_context_routes.py
          test -f tests/cocolon_context/test_code_index.py
          test -f tests/cocolon_context/test_routes.py

      - name: Set up Python 3.11
        id: setup_python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Set up Node 20
        id: setup_node
        uses: actions/setup-node@v4
        with:
          node-version: "20"

      - name: Install pinned SCIP toolchain
        id: toolchain
        shell: bash
        run: |
          set -euo pipefail
          npm install -g --no-audit --no-fund \
            @sourcegraph/scip-typescript@0.4.0 \
            @sourcegraph/scip-python@0.6.6 \
            typescript@5.2.2
          NODE_PATH="$(npm root -g)"
          export NODE_PATH
          echo "NODE_PATH=${NODE_PATH}" >> "$GITHUB_ENV"
          SCIP_TAG=v0.7.1
          curl --retry 3 --retry-all-errors -fsSL \
            "https://github.com/scip-code/scip/releases/download/${SCIP_TAG}/scip-linux-amd64.tar.gz" \
            -o /tmp/scip.tar.gz
          rm -rf /tmp/scip-bin
          mkdir -p /tmp/scip-bin
          tar -xzf /tmp/scip.tar.gz -C /tmp/scip-bin
          SCIP_BIN="$(find /tmp/scip-bin -type f -name scip -print -quit)"
          test -n "$SCIP_BIN"
          sudo install "$SCIP_BIN" /usr/local/bin/scip
          node --version
          node -e "console.log(require('typescript').version)"
          scip --version
          scip-typescript --version
          scip-python --version

      - name: Run system-context tests
        id: tests
        shell: bash
        run: |
          set -euo pipefail
          node --check tools/cocolon_context_ts_syntax.cjs
          node --check tools/cocolon_context_ts_routes.cjs
          python3 -m py_compile \
            tools/cocolon_context_inventory.py \
            tools/cocolon_context_code_index.py \
            tools/cocolon_context_routes.py
          python3 -m unittest tests/cocolon_context/test_inventory.py -v
          python3 -m unittest tests/cocolon_context/test_code_index.py -v
          python3 -m unittest tests/cocolon_context/test_routes.py -v
          python3 -m unittest tests/cocolon_context/test_routes_nested_registration.py -v

      - name: Build and verify full tracked-file inventory
        id: inventory
        shell: bash
        run: |
          set -euo pipefail
          python3 tools/cocolon_context_inventory.py build \
            --profiles Cocolon_前提資料/system_context/workspace_profiles.json \
            --workspace cmee_working \
            --repo Cocolon="${GITHUB_WORKSPACE}" \
            --repo mashos-api="${GITHUB_WORKSPACE}/.cocolon-context-workspace/mashos-api" \
            --output Cocolon_前提資料/system_context/current/cmee_working
          python3 tools/cocolon_context_inventory.py verify \
            --profiles Cocolon_前提資料/system_context/workspace_profiles.json \
            --workspace cmee_working \
            --repo Cocolon="${GITHUB_WORKSPACE}" \
            --repo mashos-api="${GITHUB_WORKSPACE}/.cocolon-context-workspace/mashos-api" \
            --output Cocolon_前提資料/system_context/current/cmee_working

      - name: Generate required precise SCIP indexes
        id: scip
        shell: bash
        run: |
          set -euo pipefail
          rm -rf .cocolon-context-workspace/scip-step2
          mkdir -p .cocolon-context-workspace/scip-step2
          python3 tools/cocolon_context_code_index.py run-scip \
            --inventory Cocolon_前提資料/system_context/current/cmee_working/files.jsonl \
            --repo Cocolon="${GITHUB_WORKSPACE}" \
            --repo mashos-api="${GITHUB_WORKSPACE}/.cocolon-context-workspace/mashos-api" \
            --work .cocolon-context-workspace/scip-step2

      - name: Build and verify SCIP plus syntax code index
        id: code_index
        shell: bash
        run: |
          set -euo pipefail
          rm -rf Cocolon_前提資料/system_context/current/cmee_working/code_index
          python3 tools/cocolon_context_code_index.py build \
            --inventory Cocolon_前提資料/system_context/current/cmee_working/files.jsonl \
            --repo Cocolon="${GITHUB_WORKSPACE}" \
            --repo mashos-api="${GITHUB_WORKSPACE}/.cocolon-context-workspace/mashos-api" \
            --scip-work .cocolon-context-workspace/scip-step2 \
            --ts-helper tools/cocolon_context_ts_syntax.cjs \
            --output Cocolon_前提資料/system_context/current/cmee_working/code_index
          python3 tools/cocolon_context_code_index.py verify \
            --inventory Cocolon_前提資料/system_context/current/cmee_working/files.jsonl \
            --output Cocolon_前提資料/system_context/current/cmee_working/code_index

      - name: Build and verify RN/API cross-repository route graph
        id: route_graph
        shell: bash
        run: |
          set -euo pipefail
          rm -rf Cocolon_前提資料/system_context/current/cmee_working/route_graph
          python3 tools/cocolon_context_routes.py build \
            --inventory Cocolon_前提資料/system_context/current/cmee_working/files.jsonl \
            --repo Cocolon="${GITHUB_WORKSPACE}" \
            --repo mashos-api="${GITHUB_WORKSPACE}/.cocolon-context-workspace/mashos-api" \
            --code-index Cocolon_前提資料/system_context/current/cmee_working/code_index \
            --rn-helper tools/cocolon_context_ts_routes.cjs \
            --output Cocolon_前提資料/system_context/current/cmee_working/route_graph
          python3 tools/cocolon_context_routes.py verify \
            --inventory Cocolon_前提資料/system_context/current/cmee_working/files.jsonl \
            --code-index Cocolon_前提資料/system_context/current/cmee_working/code_index \
            --output Cocolon_前提資料/system_context/current/cmee_working/route_graph

      - name: Validate Step 2 and Step 3 completion
        id: validate
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          import json
          from pathlib import Path
          root = Path("Cocolon_前提資料/system_context/current/cmee_working")
          inventory = json.loads((root / "manifest.json").read_text())
          code = json.loads((root / "code_index/code_index_summary.json").read_text())
          routes = json.loads((root / "route_graph/route_graph_summary.json").read_text())
          assert code["completion_claim"] == "STEP2_SCIP_AND_SYNTAX_INDEX_CONNECTED"
          assert code["inventory_total"] == inventory["total_tracked_entries"]
          assert code["coverage_total"] == inventory["total_tracked_entries"]
          assert code["coverage_gap_count"] == 0
          assert code["duplicate_coverage_count"] == 0
          assert code["required_scip_runs_ok"] is True
          assert code["scip_precise_file_count"] > 0
          assert routes["completion_claim"] == "STEP3_RN_API_CROSS_REPOSITORY_ROUTE_GRAPH_CONNECTED"
          assert routes["rn_call_count"] > 0
          assert routes["api_route_count"] > 0
          assert routes["cross_repository_edge_count"] > 0
          assert routes["silent_unresolved_count"] == 0
          PY
          git diff --check

      - name: Publish generated system-context checkpoint
        id: publish
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          du -sh Cocolon_前提資料/system_context/current/cmee_working
          find Cocolon_前提資料/system_context/current/cmee_working -type f -printf '%s\t%p\n' \
            | LC_ALL=C sort -nr \
            | awk 'NR <= 20 { print }'
          git add -- Cocolon_前提資料/system_context/current/cmee_working
          git diff --cached --check
          if git diff --cached --quiet; then
            echo "System-context outputs are already current."
            exit 0
          fi
          git commit -m "docs(context): publish code index and RN API route graph [skip ci]"
          git push origin "HEAD:${GITHUB_REF_NAME}"

      - name: Record one-shot system-context workflow failure
        if: ${{ failure() }}
        shell: bash
        env:
          OUTCOME_CHECKOUT_COCOLON: ${{ steps.checkout_cocolon.outcome }}
          OUTCOME_CHECKOUT_MASHOS: ${{ steps.checkout_mashos.outcome }}
          OUTCOME_CLEAR_FAILURE: ${{ steps.clear_failure.outcome }}
          OUTCOME_MATERIALIZE: ${{ steps.materialize.outcome }}
          OUTCOME_SETUP_PYTHON: ${{ steps.setup_python.outcome }}
          OUTCOME_SETUP_NODE: ${{ steps.setup_node.outcome }}
          OUTCOME_TOOLCHAIN: ${{ steps.toolchain.outcome }}
          OUTCOME_TESTS: ${{ steps.tests.outcome }}
          OUTCOME_INVENTORY: ${{ steps.inventory.outcome }}
          OUTCOME_SCIP: ${{ steps.scip.outcome }}
          OUTCOME_CODE_INDEX: ${{ steps.code_index.outcome }}
          OUTCOME_ROUTE_GRAPH: ${{ steps.route_graph.outcome }}
          OUTCOME_VALIDATE: ${{ steps.validate.outcome }}
          OUTCOME_PUBLISH: ${{ steps.publish.outcome }}
        run: |
          set -euo pipefail
          DIAG="Cocolon_前提資料/system_context/current/cmee_working/workflow_last_failure.json"
          FAILURE_JSON=/tmp/cocolon-system-context-failure.json
          export DIAG FAILURE_JSON
          python3 - <<'PY'
          import json
          import os
          import pathlib
          import subprocess
          keys = [
              "CHECKOUT_COCOLON", "CHECKOUT_MASHOS", "CLEAR_FAILURE", "MATERIALIZE",
              "SETUP_PYTHON", "SETUP_NODE", "TOOLCHAIN", "TESTS", "INVENTORY",
              "SCIP", "CODE_INDEX", "ROUTE_GRAPH", "VALIDATE", "PUBLISH",
          ]
          outcomes = {key.lower(): os.environ.get(f"OUTCOME_{key}", "unknown") for key in keys}
          failing = [key for key, value in outcomes.items() if value == "failure"]
          record = {
              "schema_version": "cocolon.system_context.workflow_failure_stage.v1",
              "repository": os.environ["GITHUB_REPOSITORY"],
              "branch": os.environ["GITHUB_REF_NAME"],
              "run_id": int(os.environ["GITHUB_RUN_ID"]),
              "run_attempt": int(os.environ["GITHUB_RUN_ATTEMPT"]),
              "trigger_sha": os.environ["GITHUB_SHA"],
              "worktree_head_before_reset": subprocess.check_output(
                  ["git", "rev-parse", "HEAD"], text=True
              ).strip(),
              "run_url": f"https://github.com/{os.environ['GITHUB_REPOSITORY']}/actions/runs/{os.environ['GITHUB_RUN_ID']}",
              "failing_steps": failing,
              "step_outcomes": outcomes,
              "diagnostic_scope": "ONE_SHOT_FAILURE_STAGE_ONLY",
              "product_effect": 0,
          }
          pathlib.Path(os.environ["FAILURE_JSON"]).write_text(
              json.dumps(record, ensure_ascii=False, indent=2, sort_keys=True) + "\n",
              encoding="utf-8",
          )
          PY
          git reset --hard "$GITHUB_SHA"
          git clean -fd -- Cocolon_前提資料/system_context/current/cmee_working
          mkdir -p "$(dirname "$DIAG")"
          cp "$FAILURE_JSON" "$DIAG"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -- "$DIAG"
          git diff --cached --check
          git commit -m "docs(context): record workflow failure stage [skip ci]"
          git push origin "HEAD:${GITHUB_REF_NAME}"
