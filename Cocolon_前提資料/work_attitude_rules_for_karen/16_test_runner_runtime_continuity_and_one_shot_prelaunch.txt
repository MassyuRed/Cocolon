# Work test-runner runtime continuity / one-shot prelaunch

revision_date: 2026-08-11
status: CURRENT_EFFECTIVE
effective_boundary: PHASE5_CHECKPOINT_B_ATOMIC_CURRENT_OWNER_CUTOVER_REMOTE_POSTVERIFIED
effective_status_after_boundary: CURRENT_ENFORCEMENT_RULE
current_g4b_method_effective_when: G4B_GITHUB_TRACKED_RUNTIME_ADMISSION_CHECKER_V1_EXACT11_IMPLEMENTATION_REMOTE_POSTVERIFIED

## 正本

技術定義のcurrent正本は
`../13_cocolon_work_test_runner_runtime_continuity.md`です。

このfileは、華恋が毎session・毎targeted pytest authorityで行う強制checkだけを
固定します。GitHub反映契約`../11_cocolon_github_transport_and_session_continuity.md`
またはproduct runtimeを変更しません。

Phase 5 Checkpoint Bのatomic cutoverはremote postverify済みであり、このfileは
`CURRENT_ENFORCEMENT_RULE`です。2026-08-11のcurrent G4-B checker implementation
reflectionは、tracked exact5 implementationとCocolon exact6同期だけであり、checker /
test process、runtime、Gateを許可しません。

Cycle001では、current position / blocker / next exact1は
`../08_cycle001_current_state.md`からだけ選びます。remaining Gate sequence、entry /
exit / STOP、retired routeは
`../../EmlisAIの実装済み資料/documents/NLSv3_Step11_Cycle001_CurrentClosureRoute.md`
をownerとします。`../07_latest_snapshot_diff.md`とcurrent same-name Planはevidence /
historical ownerであり、古い`next authority`からcurrent actionを選びません。

## historical G4-B direct-native-process enforcement

次はprior G4-B `G4_POST_G6_SHARED_STRUCTURAL_CORRECTION_GATE_B`へ適用されたhistorical
overrideです。このrouteはclosed / nonreusableであり、current enforcementは次sectionを
正とします。他Gateの一般runtime continuity ruleまたはhistorical observationを変更しません。

```text
method id:
GATE_B_DIRECT_NATIVE_PROCESS_ROUTE_V1

retired:
authority-local helper file
session-local helper callable
helper Path API boundary
helper repair / V3 / reuse
additional scanner
additional carrier

future technical authority process cardinality:
direct native owner process exact1
direct native independent process exact1

persisted helper / scanner / carrier:
exact0 / exact0 / exact0
```

1. future technical authorityの同じ本文でprocess body exact2をfreezeする。owner processは
   `importlib.metadata`側から導出し、independent processは`dist-info / RECORD / actual
   filesystem`側から別手順で導出する。
2. 両processが共有してよいものはfrozen comparator schemaだけとする。owner /
   independent implementation、coverage計算、filesystem traversal、intermediate stateを
   共有しない。
3. 各processへstrict UTF-8 JSON stdinだけを渡し、runtime-rootはplain JSON string field
   とする。`Path` object、abstract `Path` class、helper callable、command-line path coercionを
   process間contractにしない。
4. acquisition、materialization、readiness、pytest、role smoke、target effectより前に、
   freeze対象と同じprocess body exact2をsynthetic mini-rootへactual-callする。
5. preflightとfuture runtime derivationには同じprocess body bytesを使用する。preflight専用
   body、alternate reader、mock call、shared derivationへ差し替えない。
6. synthetic mini-rootをfixture、sample、corpus、protected test、acceptance denominatorへ
   加えず、そのbodyまたはabsolute locatorをGitHubへ記録しない。
7. 各outputをfuture authorityがfreezeするstrict schemaへ個別validateする。required /
   optional field、type、enum、cardinality、ordering、empty / `NOT_APPLICABLE`、unknown-field、
   body-freeの一項目でも不成立ならPASSにしない。exit 0またはowner-only resultを
   readinessへ変換しない。
8. preflight不成立時はruntime / target effect 0のまま
   `DIRECT_NATIVE_PROCESS_ROUTE_INVALID`でSTOPする。helper fallback、別scanner、別carrier、
   process body repair、second method repair、third normal authorityをすべて0にする。
9. comparator V2
   `NLS_V3_INSTALLED_FILE_MANIFEST_CANONICAL_V1 / 0eba095e4c173b4b69f68532fd66cf2c871ab9edef64d91754b52ed7daee15c5`、
   frozen lock、authority-specified exact5 projection、accepted wheel manifest、distribution
   closure、acceptance denominator、configured network route / count、runtime role /
   required-role orderを変更しない。
10. この2026-08-11 reflectionではprocess bodyの実装・freeze・実行を行わない。direct
    native process、runtime、pytest、network、wheel、venv、install、Gate B executionは0で、
    future technical authorityへ自動進行しない。

preflight STOP terminal:

```text
DIRECT_NATIVE_PROCESS_ROUTE_INVALID
HELPER_FALLBACK_0
ADDITIONAL_SCANNER_0
ADDITIONAL_CARRIER_0
PROCESS_BODY_REPAIR_0
SECOND_METHOD_REPAIR_0
THIRD_NORMAL_AUTHORITY_0
CURRENT_AUTHORITY_STOP
AUTOMATIC_PROGRESSION_FALSE
```

## current G4-B GitHub-tracked admission checker enforcement

```text
method id:
GATE_B_GITHUB_TRACKED_RUNTIME_ADMISSION_CHECKER_V1

tracked checker family:
exact1

responsibility / file:
exact5 / exact5

legacy helper / additional scanner / additional carrier:
exact0 / exact0 / exact0
```

1. contract、orchestrator、owner、independent、dedicated testを1責務1fileで固定する。
   existing materialization procedure exact2のordered V1 identifierは
   `COCOLON_RULE13_RUNTIME_CONTINUITY_V20260811`、
   `COCOLON_RULE16_ONE_SHOT_PRELAUNCH_V20260811`とし、別label、逆順、historical labelを
   freshness evidenceへ使用しない。
2. ownerとindependentは別file、別OS process、別PID、別call graph、別導出とする。
   導出関数、filesystem traversal、coverage計算、intermediate stateを共有しない。
3. 共有はversioned immutable contract / schemaと同一raw inputだけとする。
4. checkerはRule 13 / Rule 16のexisting procedureでexternal materialization済みのfresh
   instanceだけを受け取り、acquisition / materialization / install / downloadを行わない。
   historical nonexistence / completionはexisting procedure exact2のexternal attestation ownerとし、
   checker単独のfreshness proofへ変換しない。root locator / expected full-rootを含むversioned
   materialization eventを再導出し、actual orchestrator / owner / independent full-rootのexact matchを
   要求する。
5. strict UTF-8 JSON stdin / stdoutだけを使用し、external service、daemon、CI control
   plane、DB、API、新transport、stdlib外dependencyを追加しない。
   admission-credit surfaceはunmodified tracked CLI exact1に限定し、library invocation、runner
   injection、monkeypatch、fake process resultをcreditへ変換しない。actual child PIDとchild result
   PIDを照合し、ordered stage ledger exact4からcountを導出する。
   parent argvはrepo-root cwdの`python -E -s -S -B -m <checker-module>`だけを許可し、checkerが
   flags / module spec / cwdを再確認する。delta packed HEADまたはunsupported Git formはrepack / git
   subprocessへfallbackせずtyped STOPする。
6. checkerはtarget filesystemをread-onlyで扱い、mkdir、write、delete、repair、retry、
   fallback、alternate interpreterを行わない。
7. ownerはdistribution-first、independentはfilesystem-first reverse ownershipで別導出する。
8. input schema、freshness、frozen identities、owner、pytest probe、role smoke、independent、
   reconciliation / post-root、same-instance handoffの固定順でfail-closed判定する。
   HEAD / treeはactual Git metadata、tracked index、actual tracked bytesからread-only再導出する。
   formal lock tracked JSONのactual raw SHA-256もpre / smoke後 / postでfrozen identityへ照合する。
   role smokeのeffect 0はfilesystem write / process / network deny observationとsource pre/post一致を
   根拠とし、literal counterだけを根拠にしない。
9. same-instanceは`HANDOFF_BOUND_CURRENT_SESSION`までをGate B claimとし、future Gate Cの
   直前再導出とexact executable launchなしに`HANDOFF_CONSUMED`をclaimしない。
   current-session callerがprivate locatorを保持するbindingであり、receiver processによるfuture
   consumptionをclaimしない。
   binding preimageへabsolute root / executable locator SHA-256を含め、locatorまたはその単体digestを
   public resultへ出さない。
10. production EmlisAI source、100件runner、sample、fixture、corpusを変更・import・実行
    せず、100件生成・実行・評価を0にする。
11. implementation reflectionとverification / live admissionを分離する。本reflectionでは
    checker / dedicated test process、synthetic actual-call、network、runtime、wheel、fresh
    root、venv、install、pytest probe、role smoke、target、Gate B executionを0にする。
12. implementation reflectionではRuntime READY false、readiness ID `NOT_DERIVED`、Gate B
    open、credit 0を保持し、別のMash様明示承認なしにverification、live admission、Gate Cへ
    進まない。

current STOP terminal:

```text
METHOD_OVERRIDE_NOT_AUTHORIZED
BASE_OR_PREIMAGE_DRIFT
PATH_BUDGET_OR_SCOPE_EXCEEDED
PROHIBITED_EFFECT_REQUIRED
INPUT_SCHEMA_INVALID
SCHEMA_OR_VERSION_INVALID
FRESHNESS_UNPROVED
PAST_ARTIFACT_REUSE_DETECTED
PROCESS_CARDINALITY_OR_LAUNCH_INVALID
OWNER_INVALID
PROBE_OR_SMOKE_INVALID
INDEPENDENT_INVALID
DERIVATION_DIVERGENCE
ROOT_DRIFT
HANDOFF_INVALID
PRIVACY_VIOLATION
INTERNAL_FAIL_CLOSED
CURRENT_AUTHORITY_STOP
AUTOMATIC_PROGRESSION_FALSE
```

## 華恋が必ず行うこと

1. Python / pytest test-runnerの発見、materialization、verification、
   rediscovery、rematerializationの実行ownerは華恋とする。
2. session、workspace root、Work environmentが変わったら、過去のREADYをcurrentでも
   有効と仮定せず、`CURRENT_CONTINUITY_UNVERIFIED`へ戻す。過去readiness自体は
   履歴として保持する。
3. pytestを必要とするtargeted authorityを出す前に、Gate Aのread-only discoveryで
   candidate discovery scope、root、entrypoint、symlink/control、manifest、
   lock / projection identityを確認する。
4. candidateがなければ`RUNTIME_NOT_FOUND_AT_DECLARED_DISCOVERY_SCOPE`、
   不一致なら`RUNTIME_IDENTITY_INVALID_AT_DECLARED_DISCOVERY_SCOPE`として、
   scopeを明記し、targeted invocation 0のままSTOPする。
5. cross-session candidateの静的identityが一致しても
   `CROSS_SESSION_REDISCOVERED_PENDING_READINESS`とし、Gate Bのexact version
   probe、required role smoke、fresh identity再導出がVALIDになるまでREADYにしない。
6. runtime未発見をMash様のabsolute path、venv、wheel、pytest installation、
   runtime directory提供作業へ変換しない。
7. rematerializationはfrozen lockとGitHub-tracked procedureから行う別authority
   候補として華恋が整理する。current G4-B以外でtracked helperがない場合は
   authority-bound local helperのraw hash / countとprocedure identityを固定する。
   current G4-Bでは上記overrideに従いtracked checker family exact1を使用し、legacy helper /
   additional scanner / additional carrierをexact0とする。
   新instanceのreadinessを再確立してSTOPする。
8. `python -m pytest --version`もpytest process invocationとして数え、
   targeted invocationとは別のreadiness authority / counterにする。
9. role import probeもrepository code executionとして別counterにする。
10. targeted authorityのactivation / admission / consumption直前に、同じruntimeの
    static identityを再導出する。
11. targetは確認済みabsolute entrypointを直接launchする。generic `python`、
    `python3`、`py`、`/usr/bin/env python`、PATH search、`which`、`command -v`、
    default interpreter、fallback、interpreter switch、hidden installation、
    admission後のversion probeを行わない。
12. exact admitted absolute executableのlaunch requestをOSへ渡した時点で
    authority consumption exact1とする。OS rejectやpytest-start exact0でも巻き戻さず、
    retry / fallback / 別interpreter launchを0にする。
13. prelaunch不成立時はtargeted invocation 0のままGate Aまたは別Gate Bへ戻す。
14. historical Result / Receipt / Handoffを遡及変更・再parse・再creditせず、
    誤ったfuture directionはadditive correctionで非current化する。
15. current G4-Bでは上記GitHub-tracked admission checker overrideを使用し、items 16〜21の
    helper / launcher branchを適用しない。それ以外でhelper / launcherをfreezeする前に、
    actual entrypoint ownerとcallable、positional /
    keyword-only parameter、required / optional / default、exact invocation、return shapeを
    actual implementationから導出し、call signatureをpre-freezeする。
16. freeze対象と同じactual entrypointをauthority-local synthetic file exact1で実callする。
    import、symbol存在、mock callだけでPASSにしない。
17. synthetic fileはproductionと同じreader / call chain / argument constructionで実読する。
    alternate readerやpreflight shortcutで置換しない。
18. synthetic raw bytes、byte count、SHA-256、strict UTF-8、newline form / count、final LF、
    normalization expectationを固定し、actual text readとの一致を確認する。
19. `Path.read_text`等のAPI引数をactual callで確認し、unsupported argumentをfreezeしない。
20. actual outputをrequired / optional key、type、enum、cardinality、ordering、empty /
    NOT_APPLICABLE、unknown-field、body-freeのfrozen schemaへvalidateする。
21. items 15〜20の一つでも不成立ならfreezeせず、runtime / target effect 0のまま
    `PREFREEZE_EXECUTABLE_PREFLIGHT_INVALID`でSTOPする。

## GitHubへ残すもの / 残さないもの

GitHubへ残すのは、lock、body-free identities、path-free locator contract、
relative executable path、persistence class、expiry trigger、tracked materialization /
verification procedure identity、tracked helperまたはauthority-bound local helper identity、
rematerialization手順です。

current G4-Bでは、tracked checker family exact1のrepository-relative path、Git blob / raw
hash、contract version、logical responsibility exact5、owner / independent separate-process
identityをbody-freeで記録します。legacy helper / additional scanner / additional carrier
identityは新規作成・保存しません。本implementation reflectionではprocess execution
identity、runtime identity、readiness observation IDは未作成です。

absolute Work path、scratch / session identifier、runtime / venv本体、wheel body、
cache、credential、configured route、raw acquisition outputは残しません。

## STOPの意味

STOP自体を失敗と扱いません。runtime問題をtargeted one-shot消費前に検出し、
Work側recoveryへ戻すことが正しいSTOPです。targetを消費した後でMash様へ
environment準備を求める状態を繰り返しません。

## bounded mechanical repair / no-retry boundary

bounded mechanical repairは、prior authorityを閉じた後のnew Mash-approved authority
exact1だけで使用します。同authority retryではありません。

historical G4-Bは、同じhelper API / runtime type blocker familyのsecond failureにより
`DETOUR_RISK_STOP`へ到達し、helper routeがretiredです。したがって以下の一般mechanical
repair sequenceはcurrent G4-Bへ適用せず、helper repair、second method repair、third normal
authorityを0にします。closed direct-native routeを再利用せず、current tracked checkerにも
approval外のrepair / retry / fallbackを追加しません。

対象は`SYNTAX_ERROR`、`API_ARGUMENT_ERROR`、`SERIALIZATION_FORMAT_ERROR`、
`COMMAND_CONSTRUCTION_ERROR`のexact4です。

次の全条件を満たします。

- helperまたはlauncher exact1だけを修正する。
- same Gate / purpose / contractを維持する。
- semantics、test意味、acceptance、production、protected test、fixture、sample、
  target、denominator、comparator、input identityを変更しない。
- dependency projection、network route / count、runtime roleを変更しない。
- prior authority、root、wheel、helper、readinessを再利用しない。

許可sequence:

```text
new authority exact1
-> repair exact1
-> pre-freeze preflight
-> fully fresh root / wheel / helper
-> same Gate fresh rerun exact1
-> success or STOP
```

fresh rerunはretry例外ではありません。new authorityとfully fresh artifactsを持つ
別operationです。second failureではrepair / rerun / fallbackを追加しません。

```text
DETOUR_RISK_STOP
NO_SECOND_REPAIR
CURRENT_AUTHORITY_STOP
```

target authority consumed後のfailure、semantic / test failure、identity / comparator /
denominator conflict、dependency / network / runtime-role変更はrepair対象外で、no retryです。

```text
CURRENT_EFFECTIVE
PHASE5_CHECKPOINT_B_ATOMIC_CURRENT_OWNER_CUTOVER_REMOTE_POSTVERIFIED
CURRENT_G4_B_METHOD_GATE_B_DIRECT_NATIVE_PROCESS_ROUTE_V1
TECHNICAL_EXECUTION_0
METHOD_REFLECTION_GITHUB_PUBLICATION_REMOTE_POSTVERIFIED
AUTOMATIC_PROGRESSION_FALSE
```

CURRENT G4-B PREPARATION INGRESS PRECEDENCE — V7

Current G4-B uses controller-family V1 with platform-opened exec-time inherited read-only FD mapping, followed by tracked checker V1. A later live launch must freshly establish the platform source-open/FD9 surface and every other one-shot precondition under separate Mash Level-3 approval. V6/V7 runner, archive, root, wheel and runtime observations are not reusable as later-live evidence.

```text
CURRENT_PRECEDENCE_EFFECTIVE_DATE = 2026-08-12
CURRENT_NAVIGATION_OWNER = Cocolon_前提資料/08_cycle001_current_state.md
CURRENT_SELECTED_METHOD = GATE_B_RUNTIME_PREPARATION_CONTROLLER_FAMILY_V1_USING_PLATFORM_OPENED_EXEC_TIME_INHERITED_READ_ONLY_FD_MAPPING_V1_THEN_GITHUB_TRACKED_RUNTIME_ADMISSION_CHECKER_V1
FUNCTIONAL_PREPARATION_STATE = CONTROLLER_FAMILY_V1_FUNCTIONAL_EXACT7_WITH_PLATFORM_OPENED_EXEC_TIME_INHERITED_READ_ONLY_FD_MAPPING_V1_IMPLEMENTED_VERIFIED_REMOTE_POSTVERIFIED
LIVE_RUNTIME_EXECUTION_STATE = UNEXECUTED
CURRENT_BLOCKER = PLATFORM_EXEC_TIME_FD_MAPPING_SURFACE_AND_ALL_OTHER_ONE_SHOT_LIVE_PRECONDITIONS_NOT_FRESHLY_ESTABLISHED
CURRENT_AUTHORITY = NONE
LATER_LIVE_AUTHORITY = NOT_AUTHORED_NOT_APPROVED
NEXT_EXACT1 = NLS_V3_STEP11_CYCLE001_G4_GATE_B_PLATFORM_EXEC_TIME_FD_MAPPING_SURFACE_AND_ONE_SHOT_LIVE_NETWORK_RUNTIME_READINESS_ADMISSION_CANDIDATE
NEXT_CANDIDATE_CLASS = TECHNICAL_AUTHORITY_CANDIDATE
NEXT_CANDIDATE_SCOPE = LEVEL_3_MASH_DECISION_AND_APPROVAL_REQUIRED_SCOPE
NEXT_CANDIDATE_STATE = DEFINED_INACTIVE_SEPARATE_MASH_APPROVAL_REQUIRED
RUNTIME_READY = FALSE
READINESS_OBSERVATION_ID = NOT_DERIVED
GATE_B = OPEN
READINESS_GATE_B_TECHNICAL_PRODUCT_CREDIT = 0 / 0 / 0
GATE_C = NOT_AUTHORIZED
AUTOMATIC_PROGRESSION = FALSE
```

V7の`TECHNICAL_CREDIT`はconsumer-side source / contract / portable test / actual-DACの再利用可能な補正証拠だけを指します。Gate-B admissionのtechnical creditではなく、上記のGate creditは`0 / 0 / 0`のままです。

Evidence:
- mashos-api final head / tree: `99afecb1a30880bf42b9fde4932e5bba7e01e7d4` / `6f92113264ffef515bd2feba3c7e8ba82d0c0188`
- qualified-runner Actions run / job: `31608210201` / `94152538969`
- V7 Decision: `EmlisAIの実装済み資料/documents/NLSv3_Step11_Cycle001_G4B_QualifiedRunnerV6PostimageVerificationAndPublicationV7_Decision_20260812.md`
- V7 Receipt: `EmlisAIの実装済み資料/documents/NLSv3_Step11_Cycle001_G4B_QualifiedRunnerV6PostimageVerificationAndPublicationV7_BodyFree_Receipt_20260812.json`

CURRENT G4-B MINIMUM PREFLIGHT PRECEDENCE — 2026-08-13

Mash様のcurrent explicit instructionにより、V7 preparation ingressのcurrent operationを
置換します。checker / controller / FD codeは保持し、追加開発を停止します。prior G4-Bは
DETOUR_RISK_STOPです。

確認対象はcommit、Python/dependency、runner起動、private body保護、結果保存だけです。
一回限りpreflight PASS後は新しい安全装置を追加せずG4-Cへ進みます。このoperationの
actualはG4-C/G5/G6/G8/G9/G10まで完了し、Cycle001はNOT_ACCEPTEDです。

```text
MINIMUM_PREFLIGHT_PASS
CHECKER_CONTROLLER_FD_CHANGED_0
PRIVATE_BODY_PUBLICATION_0
G8_EXACT100_SAVED
G9_ALL100_READ
G10_RECOMPUTED
CURRENT_AUTHORITY_STOP
```
