---
doc_id: cocolon_emlis_nls_v3_step11_cycle001_recovery_epoch004_d1v5_pytest_unavailable_runtime_readiness_reconciliation_handoff
title: "Recovery Epoch004 D1 v5 pytest-unavailable runtime-readiness reconciliation handoff"
revision_date: "2026-07-31"
status: "READ_ONLY_HANDOFF_RUNTIME_READINESS_SUCCESSOR_DEFINED_INACTIVE"
body_free: true
automatic_progression: false
---

# Recovery Epoch004 D1 v5 pytest-unavailable reconciliation handoff

## 0. Authority, result, and completion gate

Approved Design-only authority:

```text
NLS_V3_STEP11_CYCLE001_RECOVERY_EPOCH004_D1_V5_POSTPUBLICATION_RUNNER_COMMAND_EXACT1_PYTEST_MODULE_UNAVAILABLE_FRAMEWORK_ENTRY_COLLECTION_EXECUTION_RUN_CHALLENGE_LIVE_REMOTE_EXACT0_NONCREDIT_TEST_RUNNER_RUNTIME_READINESS_AND_DISTINCT_ONE_SHOT_AUTHORITY_BOUNDARY_RECONCILIATION_DESIGN_READ_ONLY_AND_POSTVERIFICATION_ONLY
```

Result:

```text
published D1 v5 exact1:
RETAINED_IMMUTABLE_CURRENT_SOURCE

runner command invocation:
exact1

pytest framework entry / target import / collection / execution:
exact0 / exact0 / exact0 / exact0

run challenge:
NOT_OBSERVED

eligible live remote / O01–O08:
exact0 / all NOT_OBSERVED

prior execution permission:
CLOSED

test-runner readiness successor:
DEFINED_INACTIVE_SEPARATE_MASH_APPROVAL_REQUIRED

later corrected D1 v5 one-shot:
UNISSUED_CONDITIONAL_ON_CREDITED_RUNTIME_READY

Reference / OperationalAdmission:
BLOCKED

automatic progression:
false
```

This authority changes Cocolon documentation only.  It changes no mashos-api
source, test, fixture, dependency, configuration, or lock; acquires no package
bytes; materializes no runtime; enters no pytest framework; and performs no
live remote-main acquisition or product effect.

Completion requires the approved Cocolon NEW exact3 and append-only MODIFY
exact2 to be reachable on main, every final target exact-content postfetch
equal, Karen's write-commit aggregate unique changed-path set exact5, Plan and
latest containing the final identities, and all prefix bytes of both append
targets unchanged.  Partial publication is non-credit.

## 1. Where this reconciliation is recorded

The durable form is a combination, not one location alone:

```text
NEW exact3:
Design / Body-free Receipt / Handoff

MODIFY append-only exact2:
Execution and Closure Plan / Cocolon_前提資料 latest snapshot
```

The Design owns the full normative contract.  The Receipt owns the
machine-checkable body-free decision and publication identity.  This Handoff
owns the next-session boundary.  Plan owns sequence/history, while
`Cocolon_前提資料/07_latest_snapshot_diff.md` owns only the current premise
delta.

No other premise, work-rule, index, or manifest changes because application
structure, public owner, DB/API/RN contract, and required-reading order did
not change.

## 2. Fixed repository and source identities

```text
Cocolon entry commit / tree:
c847e9a2508cd34210d9c76db152ca502a53c2a8
05ca6b93829711f7e08977d5fbd86c6433906848

mashos-api commit / tree:
37eee88c431d1af3f8d2e96f9b0dd8b3d3bc327f
3891b84164ba0063136e47beb93d36798587a568
```

Published D1 v5:

```text
path:
ai/tests/test_emlis_nls_v3_recovery_epoch004_operational_admission_v2_event1_connection_actual_git_identity_parent_phase3_red.py

blob / raw:
c0eb936690a3423ac4615a9aabb37c40cc257324
3536b8a838ffe2ccbe29db69e9c5400c719de8e63ddf83da9ea0f83b94f17d14

bytes / lines:
416501 / 12005
```

Existing formal runtime lock:

```text
path:
ai/configs/emlis_nls_v3_recovery_epoch002_formal_worker_bootstrap_lock_v1.json

blob:
0822fcb010985cd0d384f250a9e8a1fe16dc8fd4

raw / logical:
9bb2875541a6d959c1dca47cb5b96de5b0041ccf5288e849c469c15a8b310787
801ba54efc0f6655238d14e7c153fb70b555801489aa8ba028515fc64d9c05f4

target / full distribution count:
CPython 3.12.13 / Linux x86_64 / pip 26.0.1 / exact46
```

## 3. Confirmed non-credit branch

The selected CPython 3.12.13 binary was invoked once and produced the safe
typed terminal:

```text
PYTEST_MODULE_UNAVAILABLE_FOR_SELECTED_INTERPRETER_BEFORE_FRAMEWORK_ENTRY
```

The selected binary raw SHA-256 was
`9ed008e5a8685235361f0c53771b520ab082dd99a877ad2fd796a93fa4c0b488`.
This is historical failed-run evidence, not a required binary for R0.

Because pytest framework entry was exact0, no target import, collection,
exact8 execution, D1 preflight, challenge, eligible remote observation, or
O01–O08 result exists.  The result cannot prove either a D1 semantic defect or
a causal RED vector.  It also cannot be re-run under the closed authority.

The D1 GitHub publication remains valid.  The non-credit command branch does
not revert or republish the test.

## 4. Reconciled test-runner scope

Static inspection of the D1 and owner/independent/parent import graph found
`pytest` and `packaging` as the relevant third-party surface before public API
use.  The formal exact46 lock derives this pytest-runner exact5:

| Distribution | Version | Wheel raw SHA-256 |
|---|---:|---|
| iniconfig | 2.3.0 | `f631c04d2c48c52b84d0d0549c99ff3859c98df65b3101406327ecc7d53fbf12` |
| packaging | 26.2 | `5fc45236b9446107ff2415ce77c807cee2862cb6fac22b8a73826d0693b0980e` |
| pluggy | 1.6.0 | `e920276dd6813095e9377c0bc5566d94c932c33b27a3e3945d8389c374dd4746` |
| pygments | 2.20.0 | `81a9e26dd42fd28a23a2d169d86d7ac03b46e2f8b59ed4698fb4785f946d0176` |
| pytest | 8.4.1 | `539c70ba6fcead8e78eebbf1115e8b589e7565830d7d006a8723f19ac8a0afb7` |

Canonical projection SHA-256:

```text
f501025c1dccef68c47c0a3e52f3ef74d01233f371b16f2b1a0bdfb21089e57e
```

R0 may inspect only already-configured package routes and accept bytes only
when exact filename/raw hash equality holds.  Acquisition is non-credit input
preparation.  Unconfigured sources, credential extraction/request, sdists,
builds, and dependency substitution remain forbidden.  Package-index access
ends before hashes-required, wheel-only, no-index materialization.

No eligible wheel bytes were found during this Design authority.  That fact
does not prove configured acquisition is unavailable and does not assign
package ownership to Mash.  R0 records `INPUT_UNAVAILABLE` and stops if it
cannot acquire eligible exact5 bytes.

## 5. R0 runtime-readiness boundary

R0 remains outside the D1 one-shot.  It allows only:

1. source/lock/projection preflight;
2. configured-route, hash-gated candidate wheel acquisition;
3. fresh exact5 isolated materialization;
4. one isolated pytest-version probe;
5. one isolated exact3 role smoke probe with public API calls exact0;
6. owner and independent rederivation from one immutable observation; and
7. Result, Body-free Receipt, Handoff, Plan append, and latest append.

During R0:

```text
D1 target import / collection / execution:
exact0 / exact0 / exact0

challenge / eligible live remote / O01–O08:
exact0 / exact0 / exact0

mashos-api changed path:
exact0

product runtime / Readiness / Failure / Reservation / Attempt / formal exact134:
exact0 / exact0 / exact0 / exact0 / exact0 / exact0
```

The strict R0 receipt uses a canonical exact10 readiness observation,
separately bound owner/independent verdicts, deterministic failure null/0/
`NOT_STARTED` projections, and a delete-self receipt hash.  Raw output,
exception/environment bodies, absolute paths, package bodies, and secrets are
excluded.

Runtime identity covers the selected executable, locked distribution and
RECORD closures, installed exact5 files, and the complete retained runtime
root including control bytes, modes, symlink identities, and unexpected
entries.  Unowned importable and unexpected entry counts must both be zero.

R0 ends in exactly one typed state:

```text
RUNTIME_READY
RUNTIME_NOT_READY
```

Both stop.  Publication/postfetch failure is outside either credited terminal
and is a non-credit checkpoint stop.

## 6. Frozen but unissued R1 one-shot boundary

R1 has concrete token count exact0.  Only a credited R0 `RUNTIME_READY` may
allow Karen to design and request separate approval for it.

Immediately before any future R1, owner and independent verifier must
separately rederive the complete R0 runtime identities and executable hash.
The operational retained executable is used directly; generic `python`/PATH
resolution, interpreter switch, or rematerialization is forbidden.

The frozen invocation form is:

```text
<R0_RETAINED_EXECUTABLE> -B -m pytest -q --noconftest \
  -p no:cacheprovider <published D1 v5 path> --maxfail=0
```

R1 would have a fresh challenge, new command budget, launcher exact1,
eligible live remote-main acquisition exact1, additional owner/independent/
parent/harness live queries exact0, and retry/fallback/prior-run reuse exact0.
Expected credit remains O01–O07 `CAUSAL_RED`, O08
`GREEN_V1_INVARIANCE`, and closure exact1.  Any other result stops non-credit
without a same-authority rerun.

## 7. Current Design publication evidence

The Design had one pre-Receipt corrective publication.  The intermediate was
postfetch equal but is non-credit and superseded before Receipt:

```text
intermediate commit / tree / blob / raw:
90bad3d885d5d49c3b04928348e1398b3657a10a
94b11258bff6b4248107d6370daf5df24db0b29e
cef8b81ff2e9078e4bea094081b2c4e245ce51f0
59e005b0bc7dc627b3757d4e2e6cb1116f5cf81b7e13b469daa7767e798e0715
```

Final Design:

```text
commit / tree / blob / raw / bytes / lines:
5b198cdb61c9d6f2bf95f263084d76d88246d1f3
ce29b29dfcc80513872c6f8bb418bc873aa5ff23
94fda6f067e5b63f09476fc895d7906958a55202
775ab3df619d196fef1626f7baa48147cc40deeee52457e341ff3560ff15e28e
36320 / 827
```

Body-free Receipt:

```text
commit / tree / blob / raw / bytes:
c3dabbd54a7fd7e57db2d81a8f2d218a99d10a0c
11f3765c8983462d04a087039caa17ddbf217147
c4d47ee4cfbb044a1d8ca2f6b850c13f3f7e120d
a6b6340b1f8878492c0b6436b3a8d0531d58521a952342d5f1108e8595de8fdf
7662

delete-self logical:
403e3e5e8247f9cec419f007799efc6300a691e37baf2567a1572868d6076133
```

Both final inputs are exact-content postfetch equal.  Receipt bytes are
canonical compact sorted UTF-8 JSON plus LF exact1 and do not contain their
own publication identity.

## 8. Publication and responsibility boundary

Current order is:

```text
final Design -> Receipt -> this Handoff -> Plan append -> latest append
```

This Handoff binds final Design and Receipt identities.  Plan will bind the
published exact3, and latest will bind the prior exact4 and complete exact5.
Every write commit changes one approved path; the aggregate unique path set is
exact5.  Plan/latest entry prefixes remain byte-identical.

Three read-only subagent lanes inspected scope, runtime boundary, and receipt
contract.  They made no edit, commit, GitHub write, pytest collection/
execution, repo-module import, package acquisition, or live remote
observation.  Karen retained final judgment, corrected every blocker, and
performed every GitHub write.

## 9. Confirmed facts, inference, and Karen's opinion

### 9.1 Confirmed facts

- D1 v5 exact1 remains published at fixed bytes;
- the prior runner command was invoked exact1, but pytest framework and D1
  execution boundaries were never reached;
- the selected historical interpreter lacked the pytest module;
- the formal exact46 lock derives an exact5 runner projection;
- no eligible wheel was found in the current session;
- same-authority rerun is forbidden and did not occur; and
- corrected D2, stability, Reference/OA, and product stages remain blocked.

### 9.2 Inference

The direct evidence shows an interpreter/runner selection gap, not a D1 source
defect proved by pytest.  The exact5 projection is likely enough to close the
known launcher/import uncertainty, but only R0 can establish that.  It does
not predict an O01–O08 vector.

### 9.3 華恋の意見

公開済みD1を戻したり、`pytest`だけを場当たり的に足したりせず、同じ実行体を
後続one-shotまで保持できることを先に証明します。ただしformal worker用の
exact46全体は要求せず、今回必要なexact5だけに限定するのが妥当です。

readinessの失敗はO01–O08に混ぜません。D1の因果証拠は、fresh challengeと
actual-Git exact1を持つ一回のexact8からだけ受け取ります。

## 10. Exactly one next authority and stop

```text
next authority:
NLS_V3_STEP11_CYCLE001_RECOVERY_EPOCH004_D1_V5_PUBLISHED_EXACT1_POSTFETCHED_PYTEST_MODULE_UNAVAILABLE_BEFORE_FRAMEWORK_ENTRY_PRECOLLECTION_NONCREDIT_CONFIGURED_ROUTE_HASH_VERIFIED_LOCK_DERIVED_EXACT5_ARTIFACT_ACQUISITION_TEST_RUNNER_MATERIALIZATION_PYTEST_AND_ROLE_IMPORT_PROBES_INDEPENDENT_RUNTIME_READINESS_VERIFICATION_READY_OR_NOT_READY_STOP_ONLY

state:
DEFINED_INACTIVE_SEPARATE_MASH_APPROVAL_REQUIRED

concrete token count:
exact1

later R1 concrete token count:
exact0

automatic progression:
false
```

Required stop:

```text
RECOVERY_EPOCH004_D1_V5_PUBLISHED_EXACT1_POSTFETCH_VERIFIED_RUNNER_COMMAND_EXACT1_PYTEST_MODULE_UNAVAILABLE_BEFORE_FRAMEWORK_ENTRY_NONCREDIT_FRAMEWORK_ENTRY_TARGET_IMPORT_COLLECTION_EXECUTION_LIVE_REMOTE_EXACT0_O01_O08_NOT_OBSERVED_PRIOR_EXECUTION_PERMISSION_CLOSED_TEST_RUNNER_EXACT5_READINESS_SUCCESSOR_DEFINED_INACTIVE_R1_UNISSUED_REFERENCE_OPERATIONAL_ADMISSION_BLOCKED_AUTOMATIC_PROGRESSION_FALSE_AUTHORITY_STOP
```

No R0 acquisition/materialization/probe, no R1 exact8, no corrected D2 or
stability run, and no later authority was executed.
