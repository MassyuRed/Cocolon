---
doc_id: cocolon_emlis_nls_v3_step11_cycle001_recovery_epoch004_d1v5_published_exact1_pytest_unavailable_precollection_noncredit_runtime_readiness_distinct_one_shot_authority_boundary_reconciliation_design
title: "Recovery Epoch004 D1 v5 published-exact1 pytest-unavailable precollection non-credit runtime-readiness and distinct-one-shot authority-boundary reconciliation"
revision_date: "2026-07-31"
status: "READ_ONLY_DESIGN_FROZEN_RUNTIME_READINESS_SUCCESSOR_DEFINED_INACTIVE"
body_free: true
automatic_progression: false
---

# 0. Authority and result

Approved Design-only authority:

```text
NLS_V3_STEP11_CYCLE001_RECOVERY_EPOCH004_D1_V5_POSTPUBLICATION_RUNNER_COMMAND_EXACT1_PYTEST_MODULE_UNAVAILABLE_FRAMEWORK_ENTRY_COLLECTION_EXECUTION_RUN_CHALLENGE_LIVE_REMOTE_EXACT0_NONCREDIT_TEST_RUNNER_RUNTIME_READINESS_AND_DISTINCT_ONE_SHOT_AUTHORITY_BOUNDARY_RECONCILIATION_DESIGN_READ_ONLY_AND_POSTVERIFICATION_ONLY
```

This authority records the already-observed pre-framework-entry failure,
retains the published D1 v5 bytes, freezes a test-runner runtime-readiness
boundary, and freezes the boundary of a later D1 one-shot without issuing
that later one-shot.

```text
Cocolon documentation:
NEW exact3 / MODIFY append-only exact2

mashos-api production / test / fixture / dependency / config / lock:
exact0 / exact0 / exact0 / exact0 / exact0 / exact0

package acquisition / runtime materialization:
exact0 / exact0

pytest framework entry / target import / collection / execution:
exact0 / exact0 / exact0 / exact0

live remote-main acquisition:
exact0

automatic progression:
false
```

The Design receives credit only after all approved Cocolon exact5 paths are
reachable on main, exact-content postfetch equal, Karen's write-commit
aggregate unique changed-path set is exact5, and the latest snapshot contains
the final result.  Partial publication is non-credit.

# 1. Governing context and confirmed current state

## 1.1 Governing order

The Design was prepared after re-reading, in order:

1. Karen-Diary current main and Karen's operating principles;
2. `Cocolon_前提資料` and `work_attitude_rules_for_karen`;
3. the current Epoch004 D1 v5 Design, Receipt, and Handoff;
4. the Execution and Closure Plan and latest snapshot;
5. the published mashos-api D1 v5 exact1 and existing formal runtime lock;
6. the observed non-credit command result; and
7. the three current session attachments.

GitHub reflection remains governed only by
`Cocolon_前提資料/11_cocolon_github_transport_and_session_continuity.md`
`CURRENT_NORMATIVE_CONTRACT`.  This Design adds no transport requirement.

## 1.2 Fixed repository and D1 identities

```text
Cocolon entry commit / tree:
c847e9a2508cd34210d9c76db152ca502a53c2a8
05ca6b93829711f7e08977d5fbd86c6433906848

mashos-api current commit / tree:
37eee88c431d1af3f8d2e96f9b0dd8b3d3bc327f
3891b84164ba0063136e47beb93d36798587a568

published corrected D1 v5 path:
ai/tests/test_emlis_nls_v3_recovery_epoch004_operational_admission_v2_event1_connection_actual_git_identity_parent_phase3_red.py

D1 Git blob SHA-1 / raw SHA-256:
c0eb936690a3423ac4615a9aabb37c40cc257324
3536b8a838ffe2ccbe29db69e9c5400c719de8e63ddf83da9ea0f83b94f17d14

D1 bytes / lines:
416501 / 12005
```

The GitHub publication is valid and remains the current source.  The
non-credit execution branch does not revert, weaken, replace, or republish
those bytes.

## 1.3 Exact observed branch

The approved command was invoked once against the clean published main
checkout.  Body-free evidence establishes:

```text
logical command:
python -m pytest -q <published D1 v5 path> --maxfail=0

selected interpreter:
CPython 3.12.13

selected interpreter binary raw SHA-256:
9ed008e5a8685235361f0c53771b520ab082dd99a877ad2fd796a93fa4c0b488

exit code / observed wall time:
1 / 0.0505 seconds

typed terminal:
PYTEST_MODULE_UNAVAILABLE_FOR_SELECTED_INTERPRETER_BEFORE_FRAMEWORK_ENTRY

command invocation:
exact1

pytest framework entry / target import / collection / execution:
exact0 / exact0 / exact0 / exact0

D1 preflight / run challenge / eligible live remote attempt:
exact0 / NOT_OBSERVED / exact0

O01–O08:
all NOT_OBSERVED

D1_CAUSAL_RED_REFREEZE_ESTABLISHED:
false
```

This proves only that the selected interpreter could not resolve the pytest
module.  It does not prove that every host/interpreter lacks pytest or that
the D1 source has a semantic defect.

# 2. Position-change gate

## 2.1 Prior position

The preceding reconciliation authorized published D1 v5 exact1 followed by
one full exact8.  It expected O01–O07 causal RED and O08 GREEN v1 invariance,
but did not bind the selected execution interpreter to a verified pytest
runner before opening the command invocation.

## 2.2 Corrected position

Publication remains credited.  Execution credit is non-credit and the prior
authority's execution permission is closed.  A later D1 run is allowed only
under a distinct authority and only after the exact retained interpreter and
runner that would execute it pass an independently verified readiness gate.

The readiness gate binds the existing exact46 lock by reference and uses only
its immutable pytest-runner transitive exact5 projection.  It does not
require/install the unrelated exact41, rewrite the lock, substitute a
historical runtime identity, or add a repository dependency.

## 2.3 Valid and invalidated prior claims

The new direct change basis is:

```text
PYTEST_MODULE_UNAVAILABLE_FOR_SELECTED_INTERPRETER_BEFORE_FRAMEWORK_ENTRY
```

It invalidates only the assumptions that an unverified generic Python was
sufficient and that the same authority remained available after a runner
error.  It does not invalidate:

- published D1 v5 exact1 bytes;
- the D1 semantic obligations;
- retained targeted-GREEN D2 source;
- v1 OperationalAdmission exact16 or predecessor exact8 invariants; or
- the block on corrected D2, stability, Reference/OA, Candidate/Event1,
  source lock, runtime product effects, and later stages.

# 3. Non-credit terminal and closed authority

```text
terminal class:
PRE_FRAMEWORK_ENTRY_PYTEST_MODULE_UNAVAILABLE_NONCREDIT

published source:
RETAINED

same-authority rerun / dependency installation / interpreter switch:
FORBIDDEN / FORBIDDEN / FORBIDDEN

challenge-consumption claim:
NOT_MADE

prior execution permission:
CLOSED
```

The unobserved challenge is not relabelled as reusable.  Only a separately
approved future one-shot may create a fresh challenge through the unchanged
D1 test, after credited readiness.  Prior-run observation, challenge, and
result reuse remain exact0.

# 4. Frozen test-runner input

## 4.1 Existing lock bound by reference

```text
path:
ai/configs/emlis_nls_v3_recovery_epoch002_formal_worker_bootstrap_lock_v1.json

Git blob SHA-1:
0822fcb010985cd0d384f250a9e8a1fe16dc8fd4

raw / logical SHA-256:
9bb2875541a6d959c1dca47cb5b96de5b0041ccf5288e849c469c15a8b310787
801ba54efc0f6655238d14e7c153fb70b555801489aa8ba028515fc64d9c05f4

identity class:
PIP_REQUIRE_HASHES_WHEEL_LOCK_V1

target:
CPython 3.12.13 / Linux x86_64 / pip 26.0.1

full lock distribution count:
exact46
```

This is `REFERENCE_BIND_ONLY`.  Lock bytes, package/config files, the
Epoch002 formal-worker contract, and historical Epoch003 runtime receipts
remain immutable.

## 4.2 Lock-derived pytest-runner exact5 projection

Static inspection of the published D1 and the current owner, independent,
and parent role import graph found no third-party dependency outside
`packaging` before public API use; the test itself additionally requires
`pytest`.  The referenced lock's transitive pytest runner is exact5:

| distribution | version | wheel raw SHA-256 | wheel RECORD SHA-256 | installed RECORD closure SHA-256 |
|---|---:|---|---|---|
| iniconfig | 2.3.0 | `f631c04d2c48c52b84d0d0549c99ff3859c98df65b3101406327ecc7d53fbf12` | `06144d1d47c00e05c46b66ee3503fc35022bbce0a18089371d0bcd24de9abcad` | `390ff70b72d7d6be5bf03dae9ff546d6a13584704d3078ce0000eded74092e05` |
| packaging | 26.2 | `5fc45236b9446107ff2415ce77c807cee2862cb6fac22b8a73826d0693b0980e` | `f8970ce9313b03142296fcbb8c9db6df7836e66710a71ae07b99939fc49f30cf` | `851cdb430628cce99eb887f87405c6c2bfb24e642a972cdf843267018c82c3a1` |
| pluggy | 1.6.0 | `e920276dd6813095e9377c0bc5566d94c932c33b27a3e3945d8389c374dd4746` | `e3f054e6e651035e45561a2f97427e0cd24205e6c1ee6c9c38cc59424b743c90` | `b072098f6cace7afdf1d4759f13b91f805836743a0af3c4f38f40343c60ee942` |
| pygments | 2.20.0 | `81a9e26dd42fd28a23a2d169d86d7ac03b46e2f8b59ed4698fb4785f946d0176` | `cbcfe0f4255177b43aad099d47a0551c6304fa6a3dd8d5d65d6ff0d2f53ff804` | `ce2debc2a42c4274ea75b03e9c0f4dc5bd01c6f25e86da4fac5d76296e3b1a05` |
| pytest | 8.4.1 | `539c70ba6fcead8e78eebbf1115e8b589e7565830d7d006a8723f19ac8a0afb7` | `73ca68976a7a39a5127f1ad03216d55b1f6a1bda49e509543a8288c45310237c` | `5cefca6d1f84bef673818f562c6b63b100d32b51dc26405c5bed8cbd91b11874` |

Projection identity is SHA-256
`f501025c1dccef68c47c0a3e52f3ef74d01233f371b16f2b1a0bdfb21089e57e`
over a compact, key-sorted UTF-8 JSON array with no trailing LF.  Array order
is `iniconfig`, `packaging`, `pluggy`, `pygments`, `pytest`; each row contains
exactly `normalized_distribution_name`, `distribution_version`,
`wheel_filename`, `wheel_sha256`, `wheel_record_sha256`,
`installed_record_closure_sha256`, and `selected_dependency_names`, copied
from the lock.

The fresh target receives exact5 with extra/missing target distributions
exact0.  Installer pip 26.0.1 runs outside that target.  Installation is
wheel-only, hashes-required, and no-index.

## 4.3 Current availability and acquisition ownership

Bounded inspection found no eligible wheel/wheelhouse in the current
workspace or runtime caches.  The selected interpreter has `packaging` 26.2,
but not `pytest`, `iniconfig`, `pluggy`, or `pygments`.  The three session
attachments are Markdown and contain no package bytes.

The historical exact46 offline input is known only by prior evidence:

```text
wheel count / bytes:
46 / 20617522

wheelhouse manifest SHA-256:
332df480a36d9796636e5bffe2e6bf7f6dff7aa0c95b65721b04acad14aa81c6

production wheel-bundle manifest SHA-256:
63f3915ccf57845dc0c4b5d14762207d23d1cb7a435a9de8411add8491ba6fc8
```

Those identities do not prove byte availability now, that Mash possesses the
historical bytes, or that no configured package route can supply them.

R0 therefore authorizes Karen to inspect only already-configured package
routes and to acquire candidate exact5 wheel bytes as non-credit input
preparation.  No credential extraction, new credential request, unconfigured
source, source-package build, or dependency substitution is allowed.  A byte
becomes eligible only after its filename and raw SHA-256 equal the frozen
projection.  Package-route access ends before materialization; installation,
probes, and R1 require package-index access exact0.  If no eligible route or
bytes exist, R0 records `INPUT_UNAVAILABLE` and stops.  If an exact46 bundle
is otherwise available, the extra exact41 are not installed.

# 5. R0 test-runner runtime-readiness-only boundary

## 5.1 Authority separation

```text
R0 test-runner runtime readiness-only
-> only credited RUNTIME_READY with retained identical runtime may permit
R1 corrected D1 v5 full exact8 one-shot
```

R0 is the exactly-one successor issued by this Design.  R1 is not issued.
R0 may not import/collect the D1 test or acquire live remote-main evidence.
R1 requires a separate Design/approval binding the credited R0 receipt and
the same retained runtime identity.

## 5.2 R0 required checks and cardinalities

R0 requires:

1. clean mashos-api main at commit/tree
   `37eee88... / 3891b841...`, with fixed D1 blob/raw;
2. exact lock and exact5 projection identities from section 4;
3. already-configured-route inspection and candidate acquisition remain
   non-credit preparation; accepted input has exact5 filenames/raw hashes,
   while unconfigured sources, sdists, builds, and dependency substitution
   remain exact0;
4. after accepted-input identity is frozen, package-index access is exact0;
   fresh isolated materialization is exact1, wheel-only, hashes-required,
   no-index, using pip 26.0.1 outside the target;
5. target projected distributions/versions exact5, extra/missing exact0, and
   all installed RECORD closures equal;
6. chosen interpreter is CPython 3.12.13 on Linux x86_64; R0 observes its
   executable SHA-256 and binds that value to the credited receipt and R1;
7. `PYTEST_DISABLE_PLUGIN_AUTOLOAD=1` and `PYTHONDONTWRITEBYTECODE=1`, with
   `PYTEST_ADDOPTS`, `PYTEST_PLUGINS`, and `PYTHONPATH` absent;
8. from an empty non-repository cwd, selected interpreter invocation exact1:
   `-I -B -m pytest --version -p no:cacheprovider`, proving pytest 8.4.1;
9. a separate `-I -B` smoke probe exact1 that loads only these current source
   roles from explicit paths and calls no public API:
   - owner: `ai/services/ai_inference/emlis_ai_recovery_epoch002_sequence_ledger_v3.py`;
   - independent: `ai/tools/emlis_nls_v3_recovery_epoch002_closure_receipt_verify.py`;
   - parent: `ai/tools/emlis_nls_v3_recovery_epoch002_formal_parent_orchestrator_v3.py`;
10. source cleanliness and fixed identities reverified after both probes; and
11. D1 target import/collection/execution, preflight, challenge creation,
    eligible live remote, and O01–O08 all exact0.

Absolute temporary paths, kernel release, host name, cache identity,
acquisition history, and historical runtime-root identities are not
acceptance fields.  The fresh runtime root is represented by a body-free
content identity and never substituted into historical evidence.

## 5.3 Strict body-free readiness receipt

R0 publishes a body-free receipt with top-level exact15:

```text
schema_version
authority_token
body_free
readiness_observation
readiness_observation_sha256
owner_verdict
independent_verdict
failure
state
result_publication
repository_scope
zero_effects
automatic_progression
receipt_sha256_preimage_rule
receipt_sha256
```

`readiness_observation` has exact10 nested objects:

```text
source_identity
lock_identity
runner_projection
input_identity
materialization_identity
interpreter_identity
environment_policy
probe_cardinalities
pytest_probe
role_smoke_probe
```

The nested keysets are frozen as follows:

| object | exact keys |
|---|---|
| `source_identity` | `repository_full_name`, `commit_sha1`, `tree_sha1`, `clean_main`, `d1_path`, `d1_blob_sha1`, `d1_raw_sha256` |
| `lock_identity` | `path`, `git_blob_sha1`, `raw_sha256`, `logical_sha256`, `runner_projection_sha256`, `runner_distribution_count` |
| `runner_projection` | `ordered_distribution_names`, `ordered_distribution_versions`, `canonical_rows_sha256`, `installer_version` |
| `input_identity` | `acquisition_state`, `accepted_wheel_count`, `accepted_wheel_manifest_sha256`, `configured_route_access_count`, `rejected_candidate_count`, `sdist_count`, `build_count`, `unconfigured_source_count` |
| `materialization_identity` | `state`, `materialization_count`, `runtime_root_identity_sha256`, `distribution_count`, `distribution_closure_sha256`, `record_closure_match_count`, `post_acceptance_package_index_access_count` |
| `interpreter_identity` | `implementation`, `version`, `platform`, `executable_sha256`, `pytest_version` |
| `environment_policy` | `fixed`, `removed`, `working_directory_class`, `policy_sha256` |
| `probe_cardinalities` | `pytest_invocation_count`, `role_smoke_process_count`, `role_module_load_count`, `d1_target_import_count`, `collection_count`, `execution_count`, `preflight_count`, `challenge_count`, `live_remote_attempt_count` |
| `pytest_probe` | `argv_sha256`, `invocation_count`, `exit_code`, `reported_version`, `result` |
| `role_smoke_probe` | `argv_sha256`, `invocation_count`, `ordered_role_paths_sha256`, `role_count`, `public_api_call_count`, `effect_count`, `result` |

`accepted_wheel_manifest_sha256` hashes compact key-sorted UTF-8 JSON with no
LF over the ordered exact5 `{wheel_filename, wheel_sha256}` rows actually
accepted.  `distribution_closure_sha256` uses the same rule over installed
name/version/RECORD-closure rows.  `runtime_root_identity_sha256` excludes its
absolute location.  `environment_policy.fixed` contains only the exact2 safe
name/value pins; `removed` contains only the exact3 names, not inherited
values.

The finite enums are:

```text
input acquisition_state:
ALREADY_AVAILABLE / ACQUIRED_CONFIGURED_ROUTE / INPUT_UNAVAILABLE /
INPUT_INVALID

materialization state:
NOT_STARTED / FAILED / VERIFIED

probe result:
NOT_STARTED / VALID / INVALID

verdict:
VALID / INVALID

receipt state:
RUNTIME_READY / RUNTIME_NOT_READY
```

`failure` has exact3 `class`, `stage`, and `safe_code`.  `class` is one of:

```text
NONE
INPUT_UNAVAILABLE
INPUT_HASH_MISMATCH
SOURCE_IDENTITY_MISMATCH
LOCK_IDENTITY_MISMATCH
MATERIALIZATION_FAILURE
DISTRIBUTION_CLOSURE_MISMATCH
INTERPRETER_TARGET_MISMATCH
ENVIRONMENT_POLICY_MISMATCH
PYTEST_PROBE_FAILURE
ROLE_SMOKE_PROBE_FAILURE
POSTPROBE_SOURCE_DRIFT
OWNER_INDEPENDENT_DISAGREEMENT
PUBLICATION_POSTFETCH_FAILURE
```

`stage` is `NONE`, `ACQUISITION`, `INPUT_VERIFICATION`, `SOURCE_PREFLIGHT`,
`MATERIALIZATION`, `INTERPRETER`, `PYTEST_PROBE`, `ROLE_SMOKE`,
`SOURCE_POSTCHECK`, `OWNER_INDEPENDENT`, or `PUBLICATION`.  `safe_code` is
`NONE` or the selected failure-class token; raw exception/output text is not
allowed.

`owner_verdict` and `independent_verdict` each have exact4 `role`, `verdict`,
`reason_code`, and `readiness_observation_sha256`.  `reason_code` is
`ALL_CHECKS_EQUAL` or the selected safe failure-class token.  Each verifier
independently canonicalizes the exact10 observation; neither consumes the
other's verdict.

`readiness_observation_sha256` is SHA-256 of compact key-sorted UTF-8 JSON of
only `readiness_observation`, with no trailing LF.  Both verdict hashes must
equal it.  `RUNTIME_READY` exists only if failure is `NONE`, all required
counters/identities equal, and both verdicts are `VALID` with
`ALL_CHECKS_EQUAL`.  Every other complete outcome is `RUNTIME_NOT_READY`.

`result_publication` has exact8 `repository_full_name`, `path`,
`publication_commit_sha1`, `publication_tree_sha1`, `git_blob_sha1`,
`raw_sha256`, `byte_count`, and `postfetch_exact_equal`.
`repository_scope` has exact5 `cocolon_new_path_count`,
`cocolon_modified_path_count`, `cocolon_paths`,
`mashos_api_changed_path_count`, and
`production_or_test_changed_path_count`.  `zero_effects` has exact11
`d1_target_import_count`, `pytest_collection_count`,
`pytest_execution_count`, `challenge_creation_count`,
`live_remote_attempt_count`, `o01_o08_observed_count`,
`mashos_api_change_count`, `product_runtime_effect_count`,
`reference_or_operational_admission_count`, `candidate_or_event1_count`, and
`formal_exact134_invocation_count`; all are zero.

The receipt contains enums, counts, immutable hashes, and derived verdicts
only.  It excludes raw stdout/stderr, raw environment, absolute paths,
package/test/exception bodies, and secrets.  `receipt_sha256` is SHA-256 of
compact key-sorted UTF-8 JSON with no trailing LF after deleting only
`receipt_sha256`.

## 5.4 R0 terminals and retention

```text
all checks true; owner/independent both VALID on one observation identity;
Cocolon result exact5 postfetch equal:
RUNTIME_READY

input/hash/platform/materialization/RECORD/probe/source/verdict mismatch:
RUNTIME_NOT_READY
```

Both terminals stop.  R0 does not automatically repair, substitute an index,
reuse a prior runtime, create a challenge, observe O01–O08, or proceed to R1.

`RUNTIME_READY` remains eligible for R1 only while the same session-local
runtime root and chosen executable are available and rehash equal.  Loss,
mutation, or rematerialization expires readiness and requires a new authority.

# 6. R1 frozen but unissued one-shot boundary

Only after credited `RUNTIME_READY`, a separately designed and approved R1
may bind without mutation:

- mashos-api commit/tree `37eee88... / 3891b841...`;
- D1 blob/raw `c0eb936... / 3536b8a8...`;
- the identical exact5-ready interpreter/runtime identity from R0;
- a fresh challenge created by the unchanged D1 test;
- existing repository root, expected HEAD, and expected tree pins; and
- frozen actual-Git observation and independent role-consumption contracts.

```text
full exact8 launcher:
exact1

eligible live remote-main acquisition:
exact1

owner / independent / parent / harness additional live query:
exact0

retry / fallback / prior-run reuse:
exact0 / exact0 / exact0

runtime rematerialization / interpreter switch:
exact0 / exact0
```

Immediately before R1, the operational executable path retained by R0 must
resolve to the credited executable hash.  The path is used for execution but
is omitted from body-free evidence.  Generic `python`/PATH resolution is
forbidden.  R1 freezes a new command budget and fresh challenge.  Its argv is:

```text
<R0_RETAINED_EXECUTABLE> -B -m pytest -q --noconftest -p no:cacheprovider \
  <published D1 v5 path> --maxfail=0
```

It uses the R0 environment policy.  Expected credit is O01–O07
`CAUSAL_RED`, O08 `GREEN_V1_INVARIANCE`, and closure exact1.  Any other
result, runner error, typed abort, unavailable/malformed remote observation,
OID mismatch, source drift, or closure failure stops non-credit without a
same-authority rerun.

R1 concrete authority token count is exact0 and its state is
`UNISSUED_CONDITIONAL_ON_CREDITED_RUNTIME_READY`.

# 7. Repository and product-effect boundary

R0 may materialize only a session-local test runner and publish a body-free
readiness result.  Its Cocolon reflection is:

```text
NEW exact3:
EmlisAIの実装済み資料/documents/NLSv3_Step11_Cycle001_RecoveryEpoch004_D1V5_LockDerivedExact5_TestRunnerRuntimeReadiness_Result_20260731.md
EmlisAIの実装済み資料/documents/NLSv3_Step11_Cycle001_RecoveryEpoch004_D1V5_LockDerivedExact5_TestRunnerRuntimeReadiness_BodyFree_Receipt_20260731.json
EmlisAIの実装済み資料/documents/NLSv3_Step11_Cycle001_RecoveryEpoch004_D1V5_LockDerivedExact5_TestRunnerRuntimeReadiness_Handoff_20260731.md

MODIFY append-only exact2:
EmlisAIの実装済み資料/documents/NLSv3_Step11_Cycle001_ExecutionAndClosurePlan_ReadOnly_20260723.md
Cocolon_前提資料/07_latest_snapshot_diff.md

mashos-api production / test / fixture / dependency / config / lock:
exact0 / exact0 / exact0 / exact0 / exact0 / exact0

Reference / OA / Candidate / Event1 / source lock:
exact0 / exact0 / exact0 / exact0 / false

runtime product effect / Readiness / Failure / Reservation / Attempt /
formal exact134:
exact0 / exact0 / exact0 / exact0 / exact0 / exact0
```

The test runner is not a Cocolon runtime product artifact.  A future R1 must
separately name its result-reflection exact5.

# 8. Completion order and blocked stages

1. this Design-only reconciliation;
2. separately approved R0 configured-route/hash-verified input preparation,
   readiness-only probes, and Cocolon exact5 postverification;
3. if R0 ends `INPUT_UNAVAILABLE`, stop and only then request the minimum
   necessary input/route decision from Mash;
4. only after credited `RUNTIME_READY`, separately approved corrected D1 v5
   exact8 one-shot and postverification;
5. corrected D2 mandatory direct exact3 targeted GREEN under separate approval;
6. predeclared A/B stability under separate approval; and
7. only after stable postverification, Reference/OA consideration.

Corrected D2, stability, Reference/OA, Candidate/Event1, source lock, and all
runtime product stages remain blocked.

# 9. Current Design documentation scope

```text
NEW exact3:
EmlisAIの実装済み資料/documents/NLSv3_Step11_Cycle001_RecoveryEpoch004_D1V5_PublishedExact1_PytestUnavailablePrecollectionNoncredit_RuntimeReadinessAndDistinctOneShotAuthorityBoundaryReconciliation_Design_ReadOnly_20260731.md
EmlisAIの実装済み資料/documents/NLSv3_Step11_Cycle001_RecoveryEpoch004_D1V5_PublishedExact1_PytestUnavailablePrecollectionNoncredit_RuntimeReadinessAndDistinctOneShotAuthorityBoundaryReconciliation_Design_ReadOnly_BodyFree_Receipt_20260731.json
EmlisAIの実装済み資料/documents/NLSv3_Step11_Cycle001_RecoveryEpoch004_D1V5_PublishedExact1_PytestUnavailablePrecollectionNoncredit_RuntimeReadinessAndDistinctOneShotAuthorityBoundaryReconciliation_Design_ReadOnly_Handoff_20260731.md

MODIFY append-only exact2:
EmlisAIの実装済み資料/documents/NLSv3_Step11_Cycle001_ExecutionAndClosurePlan_ReadOnly_20260723.md
Cocolon_前提資料/07_latest_snapshot_diff.md
```

No other premise/rule/index/manifest changes are needed.  The application
structure, owner, public contract, DB/API/RN boundary, and required-reading
order do not change; latest snapshot is the premise owner of this completed
current-authority delta.

# 10. Confirmed facts, inference, and Karen's opinion

## 10.1 Confirmed facts

- D1 v5 exact1 is published and postfetch verified;
- runner command invocation is exact1, while pytest framework entry, target
  import, collection, execution, run challenge, live remote, and O01–O08 are
  unobserved/exact0 as typed above;
- the existing lock binds exact46 and derives the needed runner exact5;
- no eligible wheel bytes were found in the current session; this does not
  prove that a configured acquisition route is unavailable;
- same-authority rerun did not occur and is forbidden; and
- corrected D2 and all later effects remain blocked.

## 10.2 Inference

The immediate proved gap is execution-environment selection, not a D1-byte
defect proved by pytest.  The lock-derived exact5 is likely sufficient to
remove the known launcher/import uncertainty, but R0 must prove it.  This
static-reachability inference does not predict the O01–O08 result.

## 10.3 華恋の意見

公開済みtestを戻したり、`pytest`だけを場当たり的に追加したりせず、実際に
同じinterpreterでtestを開始できることをone-shotの前に閉じるべきです。
既存exact46 lockから必要なexact5だけを固定するのが最小です。

また、readiness failureをO01–O08へ混ぜません。D1の因果証拠は、pytestが起動し、
対象exact8が本当に走った一回からだけ受け取ります。

# 11. Exactly one next authority and stop

```text
token:
NLS_V3_STEP11_CYCLE001_RECOVERY_EPOCH004_D1_V5_PUBLISHED_EXACT1_POSTFETCHED_PYTEST_MODULE_UNAVAILABLE_BEFORE_FRAMEWORK_ENTRY_PRECOLLECTION_NONCREDIT_CONFIGURED_ROUTE_HASH_VERIFIED_LOCK_DERIVED_EXACT5_ARTIFACT_ACQUISITION_TEST_RUNNER_MATERIALIZATION_PYTEST_AND_ROLE_IMPORT_PROBES_INDEPENDENT_RUNTIME_READINESS_VERIFICATION_READY_OR_NOT_READY_STOP_ONLY

state:
DEFINED_INACTIVE_SEPARATE_MASH_APPROVAL_REQUIRED

concrete successor token count:
exact1

R1 concrete token count:
exact0

automatic progression:
false
```

The successor is readiness-only and contains no D1 exact8.  It may inspect
already-configured routes and accept only hash-matching exact5 wheels, but
cannot be used until Mash separately approves it.

Required stop:

```text
RECOVERY_EPOCH004_D1_V5_PUBLISHED_EXACT1_POSTFETCH_VERIFIED_RUNNER_COMMAND_EXACT1_PYTEST_MODULE_UNAVAILABLE_BEFORE_FRAMEWORK_ENTRY_NONCREDIT_FRAMEWORK_ENTRY_TARGET_IMPORT_COLLECTION_EXECUTION_LIVE_REMOTE_EXACT0_O01_O08_NOT_OBSERVED_PRIOR_EXECUTION_PERMISSION_CLOSED_TEST_RUNNER_EXACT5_READINESS_SUCCESSOR_DEFINED_INACTIVE_R1_UNISSUED_REFERENCE_OPERATIONAL_ADMISSION_BLOCKED_AUTOMATIC_PROGRESSION_FALSE_AUTHORITY_STOP
```
