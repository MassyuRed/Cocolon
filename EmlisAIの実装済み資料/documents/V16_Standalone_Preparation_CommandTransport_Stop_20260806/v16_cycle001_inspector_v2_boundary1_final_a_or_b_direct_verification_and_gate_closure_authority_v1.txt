AUTHORITY_ID=SEPARATE_MASH_UI_ATTESTED_WORK_ULTRA_CYCLE001_INSPECTOR_V2_BOUNDARY1_FINAL_A_OR_B_DIRECT_VERIFICATION_AND_GATE_CLOSURE_AUTHORITY_V1
STATE=PROPOSED_UNAPPROVED
DATE=2026-08-09
SINGLE_USE=TRUE
AUTOMATIC_PROGRESSION=FALSE
ATTEMPT_ORDINAL=FINAL_EXACT1_FOR_CURRENT_INSPECTOR_V2_BOUNDARY_PATH

# 1. Purpose, accepted outcomes, and non-outcomes
This authority is the last attempt in the current Inspector V2 boundary path. It authorizes only work that can end in exactly one of four final classes. `OUTCOME_A` means the Inspector V2 pre-execution gate is CLOSED, every closure test in section 8 is PASS, the remaining-chain denominator stays fixed at seven, and progress changes durably from `0/7 (0%)` to `1/7 (14.3%)`, delta `+14.3 percentage points`. `OUTCOME_B_SCANNER` means an actual defect is proved in the real frozen scanner file and the only next technical authority is limited to the minimum correction of that scanner file. `OUTCOME_B_INSPECTOR` means an actual defect is proved in the real frozen Inspector V2 file and the only next technical authority is limited to the minimum correction of that Inspector file. The fourth class, `WORK_PROCEDURE_OR_ENVIRONMENT_DEFECT`, is not an accepted outcome or progress: it permanently fails and retires this Inspector V2 boundary path and prohibits any V4-or-later or same-series continuation; the only permissible later proposal is one separately approved, one-time, simpler direct-verification redesign outside this lineage.
Creating this authority, publishing lifecycle or terminal records, updating Handoff/snapshot/ledger/Plan, repairing transport/file mode/hash/read counts/output capture/package/GitHub reflection, creating or checking a diagnostic, or explaining that later work can proceed is never an outcome, technical result, boundary credit, or progress. No separate diagnostic, analyzer, scanner-of-scanner, generated inspection program, or inspection helper source may be created. Administrative lifecycle and terminal reflection are allowed only because approval and the user require the attempt and its result to be durable; they receive credit0.

# 2. Predecessor disposition and fixed boundary
Authority V2, SHA-256 `415eefed6f720330e7fc776e807f75b283f80a9df652ab764690bc2a947cf837`, remains irreversibly `CLOSED_CONSUMED_STOP`, Inspector V2 gate `OPEN_STOPPED_NOT_CLOSED`, progress `0/7`, with its prohibited-read and `TextEncoder` facts unchanged and with none of its technical reads, buffers, snippets, outputs, or local artifacts reusable here. The unapproved proposal whose SHA-256 is `a167f66946142fe29c257a92a95809d016111ce54bde0cb70ae9a4bd4ca74348` is permanently `WITHDRAWN_SUPERSEDED_UNAPPROVED_UNACTIVATED_UNCONSUMED_EFFECT0_GITHUB_WRITE0`; it cannot be approved, published, activated, consumed, reused, or cited as technical evidence. This exact-new authority is not V2 retry or activation of that withdrawn proposal. The historical review receipt's scanner stderr triple is internally inconsistent: literal `STOP INSPECTOR_FORBIDDEN_ACTIVE_CALL\n` and bytes37 deterministically hash to `6c96e04cd0944dcce44723a4a7d513a662eed5441c89e838c7a1626b1ad418b5`, not its recorded `6c96e04c800506f24e189179858415610ce14665559278d738a66907f69bc3f2`. This authority binds the recomputed literal/bytes/hash triple and records the old hash as an additive documentary supersession with technical effect0, gate effect0, and progress0; correcting this identity is not an outcome.
The fixed remaining-chain denominator is exactly seven: (1) Inspector V2 pre-execution gate; (2) one-read exact4 and standalone preparation; (3) separately approved V3 program creation; (4) V16 synthetic validation; (5) remaining Full R1; (6) runtime/product/UX evidence; (7) complete Cycle001 acceptance. This authority can credit only boundary1. Cycle001 acceptance remains `NOT_ACCEPTED` even on OUTCOME_A. Exact4, specification-source access, real Inspector execution, standalone target, program V3, V16, Full R1, runtime/product/UX, app/API/DB/RN/Safety, and mashos-api mutation are not authorized.

# 3. Approval, execution environment, and final-attempt consumption
Preapproval reads of this complete proposal for hashing, independent audit, scope review, and Mash approval review have effect0. Effective approval requires one Mash message that identifies this exact AUTHORITY_ID and exact SHA-256, states that Mash has checked the currently active UI, attests that it is this same Work session in `Ultra` mode and not Chat, and approves this authority as single-use. A different hash, shortened body, Chat execution, non-Ultra execution, or ambiguous approval does not approve it.
The execution model/environment class is `GPT-5.6-sol / WORK_ULTRA_REQUIRED` because Karen plus three independent reviewer roles are part of completion evidence and the final attempt needs the same active Work runtime. Before approval: GitHub write0, technical-source read0, execution0. After approval, the final attempt is consumed exact1 when Karen begins the first authorized postapproval entry observation in section 5; neither publication nor technical-source access is required to consume it. Consumption can never be rolled back. There is no retry, reactivation, refetch continuation, alternative runtime, alternate transport lineage, or second Inspector V2 boundary attempt.

# 4. Proposal-freeze facts and immutable technical inputs
Proposal audit observed Cocolon head/tree `92bc96b18c16c4bebea36dd8cb8ab0151935ef43 / a060583e36d8907fc18dfca3aa425ce4ad2e44bc` and mashos-api head/tree `65284fef36936d7091262e758e0cc9282909601b / d951a520b7686c5bd59fba22f7dd759a0e077981`. Proposal-freeze owner blobs are Handoff `d9c433e1f6d6198627102e1d1e7084dc67b038d2`, ledger `f2b8f87b961692f0127e67f9655436299b2f3e34`, snapshot `5afcf2ee0cf720cc8590341ea69b8538fbd8eebb`, and Plan `358c40a11c630eeb8bc1060b6924e548d9c472ba`. These heads and owner blobs are observations, not expected-old/CAS gates; head or nonconflicting owner movement alone is not STOP. At execution Karen fresh-reads latest heads and exact owner paths and rebases finite suffixes on the latest nonconflicting complete bytes under current Rule11.
Immutable scanner input: path `EmlisAIの実装済み資料/documents/V16_Standalone_Preparation_CommandTransport_Stop_20260806/v16_inspector_v2_preimport_scanner_v1.js`, Git blob `ae0648a1af4ad2f4b189bfcda27d84bce66a45c1`, SHA-256 `087c8afdcbb278d7fc0c3d01c322bd64b2c168cec05840403ff4d046a5ff1c45`, bytes `9976`, mode `100644`. Immutable Inspector V2 input: path `EmlisAIの実装済み資料/documents/V16_Standalone_Preparation_CommandTransport_Stop_20260806/v16_standalone_preparation_noninteractive_inspector_generator_v2.js`, Git blob `f45082cd3970e69f980c15e598ace3f49e404003`, SHA-256 `db7c36351070487d98c34d6d34710f25ce010bb54f33d744102e00f5fcf5fc31`, bytes `60190`, LF `1583`, CR0, final-LF true, mode `100644`. Immutable harness input: path `EmlisAIの実装済み資料/documents/V16_Standalone_Preparation_CommandTransport_Stop_20260806/v16_standalone_preparation_inspector_v2_synthetic_harness_v1.js`, Git blob `99ac7d5b996489060ef7e9f72f95d7d382009855`, SHA-256 `6950bf08242c42d96e2bab53d3dca845cec2b261298e2268cce6a52ffd9734ad`, bytes `8962`, LF `212`, CR0, final-LF true, mode `100644`. Frozen B_R contract is section 11 in path `EmlisAIの実装済み資料/documents/V16_Standalone_Preparation_CommandTransport_Stop_20260806/v16_cycle001_inspector_v2_direct_gate_closure_authority_v1.txt`, Git blob `ade058821d872cd7f645dd6fb16054a2db4c7e59`, SHA-256 `5d00c7142e3ff7041b88277c6d08cf88a5097173f251318da35168cb5e5aea08`, bytes `40458`, LF `732`, CR0, final-LF true, mode `100644`. Fixed Node is `/opt/codex/runtimes/codex-primary-runtime/dependencies/node/bin/node`, direct realpath, bytes `122466960`, mode `0755`, SHA-256 `e237a2839d0cbdc9a9a2adda1a184afc0f5b20306ffbe923af5686550472d8a8`.

# 5. Authorized GitHub paths and lifecycle
Exact-new authority path: `EmlisAIの実装済み資料/documents/V16_Standalone_Preparation_CommandTransport_Stop_20260806/v16_cycle001_inspector_v2_boundary1_final_a_or_b_direct_verification_and_gate_closure_authority_v1.txt`. Exact-new activation/preflight receipt path: `EmlisAIの実装済み資料/documents/V16_Standalone_Preparation_CommandTransport_Stop_20260806/v16_cycle001_inspector_v2_boundary1_final_a_or_b_direct_verification_activation_receipt_v1.md`. Exact-new result receipt path: `EmlisAIの実装済み資料/documents/V16_Standalone_Preparation_CommandTransport_Stop_20260806/inspector_v2_boundary1_final_a_or_b_result_receipt_v1.md`. Exact-new conditional durability-failure receipt path: `EmlisAIの実装済み資料/documents/V16_Standalone_Preparation_CommandTransport_Stop_20260806/inspector_v2_boundary1_final_durability_failure_receipt_v1.md`. Conditional scanner correction path, only on its authorized branch: `EmlisAIの実装済み資料/documents/V16_Standalone_Preparation_CommandTransport_Stop_20260806/v16_inspector_v2_preimport_scanner_v2.js`. Owner4 paths are the current Handoff and `artifact_ledger.tsv` in this directory, `Cocolon_前提資料/07_latest_snapshot_diff.md`, and `EmlisAIの実装済み資料/documents/NLSv3_Step11_Cycle001_ExecutionAndClosurePlan_ReadOnly_20260723.md`. Every exact-new path must be absent immediately before its first write. Existing scanner v1, Inspector V2, harness, B_R authority, receipts, and historical records are immutable.
After exact approval Karen begins the first authorized postapproval entry observation, which consumes the final attempt exact1, then reads current required rules, current heads, immutable metadata, exact-new absence, and latest owner4 complete bytes without reading scanner/Inspector/harness bodies. If entry is valid, issue the authority-source write request. Fresh-fetch the authority exact path and require exact approved bytes. Then create and fresh-verify the activation receipt with approval/consumption exact1, Work Ultra/not-Chat attestation, current repository pairs, authority identity, technical-source body reads0, tests0, and `PREFLIGHT_PENDING`. Activation is exact1 only after both authority and receipt exact paths are verified. Any partial or unknown response gets one current-Rule11 read-only exact-path reconciliation and no resend.
Any postapproval entry failure, including current-rule unreadability, immutable identity/mode drift, exact-new path conflict, semantic owner conflict, incomplete observation, unavailable reviewer roles, or inability to perform safe GitHub reflection, sets `PROVISIONAL_TECHNICAL_CLASS=WORK_PROCEDURE_OR_ENVIRONMENT_DEFECT`, leaves technical-source body reads0, permanently retires the path, and prohibits retry or same-series succession. Reflection may use only safely absent exact paths: if the authority path itself conflicts it is not written or overwritten; if it remains absent and unattempted it may be written once. When the result path is safely absent and reflection is available, publish and verify the result receipt and safe owner terminal suffixes recording approval1/consumption1/activation0 and the blocker; none is progress. If reflection is unavailable, apply the final no-continuation rule below.
For every authority, activation, result, conditional, scanner-v2, or owner mutation response that is not immediately conclusive, perform one current-Rule11 read-only reconciliation of that exact path and never resend the same mutation. `VERIFIED` continues only along the branch whose prerequisite it satisfies. `ABSENT` or explicit `FAILED` sets provisional Work class; a safely absent result or conditional path may then record the failure, but the failed/absent path is not retried. `UNKNOWN` after reconciliation immediately assigns `FINAL_CLASS=WORK_PROCEDURE_OR_ENVIRONMENT_DEFECT`, retires the path, and requires no-further-GitHub-write, no-further-technical-source-read, preservation/reporting of every already confirmed capture/read/review/run counter, and an exact partial-state final report; authority/activation UNKNOWN naturally has technical-body-read0. No result, conditional, scanner, or owner write may cross an unresolved UNKNOWN. If the GitHub service prevents terminal publication, the verified subset plus exact reconciliation states and final report are the complete available evidence; no preservation-only or same-series authority may repair or resume the path.

# 6. Mandatory preflight before scanner or Inspector body access
After lifecycle activation and before any scanner or Inspector body fetch, materialization, semantic read, search, snippet, or review, run one nontechnical preflight. Every preflight item is exact1, uses only synthetic bytes plus the already published authority/activation receipt, and has no repository-code import or execution. Any FAIL, timeout, truncation, missing reviewer, mode/capture/hash discrepancy, package/runtime problem, GitHub write/update failure, or incomplete child reap sets provisional Work class; any unresolved UNKNOWN follows section5's immediate final Work/no-further-write branch. Technical-source bodies remain unread and this Inspector V2 path is permanently retired.
Runtime/encoding preflight: fresh-stat and hash the fixed Node identity; use one inline Node process with explicit `require('node:fs')`, `require('node:path')`, `require('node:crypto')`, and `Buffer.from(...,'utf8')` only, never global `TextEncoder`; roundtrip a fixed ASCII-plus-Japanese synthetic payload through an absent private mode0644 file, compare bytes and SHA-256, verify CR/NUL/final-byte expectations, close/reopen once, emit one fixed bounded PASS line, and prove exit0/stderr0/complete reap. No package installation, network, npx, PATH lookup, fallback, or alternate executable is permitted.
File-capture/output preflight: capture the verified published authority Git blob through the exact generic Git-object-to-private-file primitive selected for later inputs, then require byte/hash/size equality; exercise the same direct-private stdout/stderr capture wrapper with fixed synthetic output at all three profiles: syntax timeout `10000ms`, stdout cap4096, stderr cap65536; scanner timeout `10000ms`, stdout cap64, stderr cap256; harness timeout `10000ms`, stdout cap64, stderr cap65536. Every profile must prove complete nontruncated private capture, exact exit status, timeout enforcement, and determinate child reap. The primitive and wrapper may be shell operations or existing Work functions; their commands/identities and results are recorded, but no helper source file is created.
GitHub-write preflight: authority creation and activation-receipt creation must already be fresh-verified. After the runtime/file/review preflights pass, update the same activation receipt once from `PREFLIGHT_PENDING` to a complete body-free `PREFLIGHT_PASS` record and fresh-fetch exact bytes. This tests both exact-new creation and existing-file update using currently available GitHub functions. It does not impose a particular transport, create_blob, CAS, direct-child, single-commit, full-tree, recursive-fetch, or unchanged-path requirement. Technical body access remains forbidden until this update and its exact-path verification pass.
Review-delivery preflight: Karen plus three already-live independent reviewer roles must each read, from fresh private synthetic files and through the exact delivery method planned for technical review, complete deterministic line-marked fixtures of exactly `9976`, `60190`, and `8962` bytes in nonoverlapping bounded ranges. Each role returns fixture identity, range count, total bytes, first/last markers, and full SHA-256; all values must match. The method is shared private-file access with reviewer-local bounded reads and concise findings, not body transfer through a truncated chat/tool-output field. No technical source, filename-specific exception, inspection algorithm, or diagnostic program is involved. Reviewer/model/role availability and complete delivery must be proved before technical access.

# 7. Direct technical method; no diagnostic layer
Only after complete preflight PASS, fetch each pinned scanner/Inspector/harness Git object body and the frozen B_R authority body exact1 each into four fresh retained private mode0644 files using the preflighted primitive. Verify each complete Git blob/raw SHA-256/bytes/UTF-8/CR0/NUL0/final-LF/mode identity; retain those exact private files through the attempt. There is no refetch after loss and no dependence on ephemeral controller variables. The frozen B_R authority is newly captured for this attempt; no V2 read, buffer, snippet, or review is reused. Karen reads its complete section 11 exact1 and delivers the common closed review schema plus each complete assigned B_R heading/body as four reviewer-local exact1 bounded ranges: Karen/B_R1, reviewer1/B_R2, reviewer2/B_R3, reviewer3/B_R4. Each role verifies and records contract blob, heading, delivered range, bytes, SHA-256, first/last line, and complete condition count before reviewing. Subsequent explicitly authorized syntax, scanner, harness, Karen review, and reviewer-local bounded reads use the retained copies; those consumers are authorized and counted, but an expected authorized consumer count is not itself a reason to stop. Any unplanned fetch/read/search, incomplete B_R delivery, or inability to preserve the retained copies sets provisional Work class and permanently fails this path.
No new diagnostic or analyzer is created or executed. Run the real pinned scanner itself against the real retained Inspector V2 and harness inputs, and inspect the real scanner and Inspector files directly. For source localization, Karen and each of three independent reviewers separately read the complete retained scanner and complete Inspector, dividing each file within that role into deterministic nonoverlapping bounded ranges; they return only concise body-free source locations and reasoning. No expected-answer cue, filename/hash special case, fixed output exception, allowlist keyed to this Inspector, or case-specific branch is allowed.

# 8. Ordered checks, correction branch, and gate closure
The pinned scanner v1, Inspector, and harness already have one prior published-byte syntax PASS each; this authority does not credit or rerun those historical checks. After exact identity verification, run scanner v1 once with exact Inspector/harness argv, timeout `10000ms`, stdout cap64, stderr cap256, direct private capture, and determinate reap. Require exit2, stdout0, stderr exact `STOP INSPECTOR_FORBIDDEN_ACTIVE_CALL\n`, bytes37, recomputed SHA-256 `6c96e04cd0944dcce44723a4a7d513a662eed5441c89e838c7a1626b1ad418b5`. A PASS, different terminal, timeout, output mismatch, or nondeterministic result sets `PROVISIONAL_TECHNICAL_CLASS=WORK_PROCEDURE_OR_ENVIRONMENT_DEFECT`, because it cannot prove either real-file defect.
When scanner v1 reproduces the exact known STOP, Karen and three reviewers directly inspect the complete retained real scanner and Inspector. All four independently identify the scanner decision branch and every relevant active-call site, apply the frozen scanner contract, and return one verdict: `SCANNER_ACTUAL_DEFECT`, `INSPECTOR_ACTUAL_DEFECT`, or `METHOD_CANNOT_UNIQUELY_CLASSIFY`. A real-file verdict requires unanimous4/4, the same single responsible file, exact source locations, violated contract language, and causal reasoning. A unanimous Inspector verdict sets provisional class `OUTCOME_B_INSPECTOR` and ends technical execution. A unanimous scanner verdict freezes `PROVED_CAUSE=SCANNER_ACTUAL_DEFECT` and requires the correction branch; the provisional outcome remains pending until that branch ends. Disagreement, incomplete delivery, missing location, ambiguous contract, both files implicated, or inability to choose exactly one file sets provisional Work class. `CAUSE_UNRESOLVED_STOP` and unclassified FAIL are prohibited.
On unanimous scanner defect, construct exactly one minimal scanner candidate from scanner v1: one contiguous hunk in the existing responsible predicate, no unrelated refactor/dependency/public-contract change/Inspector or harness change, no source-name/hash/fixture special case, and no relaxation beyond the proved lexical context. Karen plus all three reviewers fully review the diff and complete corrected bytes. If the change cannot satisfy the bound or is rejected, set provisional B-scanner and publish no failed candidate. If accepted4/4, freeze scanner v2 for the closure suite.
The exact B_R conditions are: `B_R1` root-only argv; fixed-root binding; no cwd/env/search/fallback; every root/parent component directory/non-symlink/direct-realpath; source regular/non-symlink with mode/size and lstat/fstat dev+ino identity; O_RDONLY|O_NOFOLLOW|O_NONBLOCK; positional whole-buffer read exact1; EOF exact1; close exact1; raw identity after close; main guard; import-side source access0; no V1 regression. `B_R2` one terminal owner in Inspector and emitted target; bounded complete canonical output before emission; writeAll offset advancement; short writes only until full capture; zero/noninteger/over-report/throw/final-short typed STOP; pre-emission stdout0; partial stdout nonauthoritative; allowlisted sanitized stderr; channel-unreportable STOP; no raw stack/path/body/secret leakage. `B_R3` exact C01 version/self declarations; C02-C04 independently converge on fixed name/ordered params/range/old hash; quote-safe templates; C03 no trim/coercion and matching close at exact final byte with closed lexical states; C04 primitive strings, one whole literal, quote-specific escaping, full consumption, exact marker; C01-C04 occurrence1/nonoverlap; exact five gaps with per-gap and aggregate equality/hash; emitted target independently re-extracted, reconstructed, and proved. `B_R4` one absolute normalized direct-realpath root; fixed source/target relative paths derive under it; target parent realpath/non-symlink/existing directory/direct-realpath; target ENOENT; one target-relative constant owns Add File header; patch-memory roundtrip; emitted-target direct write/apply/import/syntax/execute/network/child/eval/vm/cwd/env/search/private access0; harness imports QA alias once after gates; production main0; real Inspector/source/exact4/target/V3 actions0.
After reviewed scanner v2 exists, run exactly three fixed-Node syntax checks once each in order for scanner v2, Inspector alias `V16_INSPECTOR_V2_SINGLE_CREATE_CANDIDATE.js`, and harness, each with timeout10000ms, stdout cap4096, stderr cap65536, direct private capture, exit0/stdout0/stderr0, and determinate reap. Scanner-v2 syntax nonPASS goes to four-role causal adjudication; only unanimous single-file scanner proof yields provisional B-scanner. Because identical Inspector/harness bytes and fixed Node previously passed syntax, either Inspector or harness syntax nonPASS, timeout, truncation, or uncertain reap sets provisional Work class, not B.
Only after syntax3 PASS, complete B_R1-B_R4 with Karen/B_R1, reviewer1/B_R2, reviewer2/B_R3, reviewer3/B_R4. Each role reads retained Inspector and harness completely once through the preflighted method and returns every assigned condition with PASS/FAIL/UNKNOWN, nonempty source-located evidence, and findings. Reviews finish even if another review fails. Scanner/harness execution is authorized only when aggregate is completed4/PASS4/FAIL0/UNKNOWN0. On any B_R nonPASS, all four roles perform one causal adjudication from retained bytes, captured findings, and every earlier cause finding without refetch or test rerun: unanimous cumulative proof of exactly scanner alone yields provisional B-scanner; exactly Inspector alone yields provisional B-Inspector; cumulative scanner+Inspector findings, harness-only defect, disagreement, incomplete proof, unsafe access, or no unique real file yields provisional Work. No later executable runs on this branch.
After syntax3 PASS and B_R PASS4 only, run scanner v2 exact1 on retained Inspector/harness; require exit0/stderr0/stdout exact `PASS\n` (bytes5, SHA-256 `c26de83abdc9496cd1301470918ec39ecca1cf389ef0ae1c6504da1800d1c431`), timeout10000ms, caps64/256, direct private capture, and determinate reap. Only on scanner PASS run harness exact1; require assertions71, exit0/stderr0/stdout exact `PASS 71\n` (bytes8, SHA-256 `ad79e35af6b51d08ffbe246024854228e7750f41d27a622f988ef0595859c640`), timeout10000ms, caps64/65536, direct private capture, determinate reap, and post-run mutation0 for all retained inputs. Any scanner-v2 nonPASS, harness nonPASS/assertion/output/timeout/reap failure, or post-run mutation invokes the same four-role cumulative causal adjudication from retained bytes, captures, and earlier findings with no rerun: unanimous exactly-one scanner/Inspector cause yields corresponding provisional B; cumulative both/neither/harness-only/disagreement/incomplete or Work mechanics yields provisional Work. No retry, fallback, alternate Node, second correction, or downstream execution is allowed.
If every local condition passes, set `PROVISIONAL_TECHNICAL_CLASS=OUTCOME_A`: preflight PASS; retained exact4 identities; scanner v1 exact known STOP; unanimous scanner-defect proof; one bounded scanner v2 reviewed4/4; syntax3 PASS; B_R PASS4; scanner-v2 PASS exact1; harness PASS71 exact1; mutation0; Inspector execution outside harness0; specification source/target/V3 actions0; prohibited special cases0. This is not yet FINAL_CLASS and cannot close the gate or change progress until section 10 durability completes. A provisional B is technically complete when its unanimous exact-one-file proof is frozen; unsafe downstream checks are `NOT_RUN_AFTER_PROVED_FILE_DEFECT`, gate remains open, and progress remains0/7.

# 9. Provisional technical class, final class, and permitted next authority
Before durability there is exactly one `PROVISIONAL_TECHNICAL_CLASS`: `OUTCOME_A`, `OUTCOME_B_SCANNER`, `OUTCOME_B_INSPECTOR`, or `WORK_PROCEDURE_OR_ENVIRONMENT_DEFECT`. It is evidence input, not FINAL_CLASS, gate closure, GitHub technical result, or progress. A/B requires the exact proof above; a mere terminal, failed test, suspicion, individual reviewer finding, or Work incident cannot become B. Both-file, no-file, ambiguous, incomplete, harness-only, or procedural cause always maps to provisional Work. Generic unresolved STOP is prohibited.
Exactly one `FINAL_CLASS` is assigned once under section 10. Verified result receipt plus verified final Handoff converts the matching provisional class to the same FINAL_CLASS. Any durability failure/ABSENT/UNKNOWN before that conversion assigns FINAL_CLASS Work instead; this is not retroactive reclassification because no prior FINAL_CLASS existed. Multiple final classes and later reclassification are prohibited.
For final `OUTCOME_B_SCANNER`, gate is `OPEN_BLOCKED_BY_PROVED_SCANNER_DEFECT`, progress0/7, and the next approval may authorize only one minimum correction of the exact scanner path plus directly necessary fixed-suite rerun; it may not touch Inspector or add a diagnostic. For final `OUTCOME_B_INSPECTOR`, gate is `OPEN_BLOCKED_BY_PROVED_INSPECTOR_DEFECT`, progress0/7, and the next approval may authorize only one minimum correction of the exact Inspector V2 path plus directly necessary fixed-suite rerun; it may not touch scanner or add a diagnostic. For final Work, gate is `CURRENT_INSPECTOR_V2_PATH_FAILED_RETIRED`, progress0/7, no V4+, same-series, retry, or continuation authority may be created; only one separately named simplified direct-verification redesign outside this lineage may later be proposed, and its creation is not authorized here. Final OUTCOME_A alone sets gate CLOSED and progress1/7.

# 10. Durable result, one-time finalization, and current Rule11
After one provisional technical class is frozen, begin durability. For provisional A only, publish scanner v2 first at its absent path and fresh-verify its exact reviewed bytes; B/Work publishes no scanner candidate. Then create and fresh-verify the body-free result receipt. It records `PROVISIONAL_TECHNICAL_CLASS`, `FINAL_CLASS=PENDING_DURABILITY`, authority lifecycle, complete preflight/input identities, syntax/scanner/harness/B_R counters, every four-role verdict and source location without full bodies, scanner diff/identity if applicable, intended gate/progress, prohibited counters, current repository pairs, facts/inference/Karen judgment, exact next-file restriction, and the deterministic finalization rule in this section. It starts `PENDING_OWNER4_POSTVERIFICATION`; it is not final progress ownership.
Only after result-receipt verification, construct latest complete owner4 postimages as finite append-only suffixes. Snapshot and Plan are provisional reflections and embed no owner-candidate identity. Freeze final Handoff before ledger; ledger has exact9 TSV fields per row and binds authority, activation receipt, scanner v2 when present, result receipt, and pre-frozen Handoff identity. Handoff embeds no ledger-candidate identity, is written last after snapshot/Plan/ledger exact-path postverification, and states `FINAL_CLASS_ON_VERIFIED_PUBLICATION=<provisional class>`. Fresh verification of both result receipt and this final Handoff assigns FINAL_CLASS exactly once. If provisional A, that moment alone closes Inspector V2 gate and moves0/7 to1/7; B/Work leaves0/7. No authority, receipt, owner update, commit count, hash, or transport is technical progress.
For a conclusive `FAILED` or reconciled `ABSENT` durability mutation, do not retry that path or continue normal owner publication. Assign FINAL_CLASS Work, retire the path, and, only if no unresolved UNKNOWN exists and the conditional receipt path is safely absent, publish/fresh-verify the conditional durability-failure receipt once. It additively supersedes any pending result/provisional owner state, records exact verified/absent/failed paths and commits, final Work, gate retired, progress0/7, and no same-series successor; after it, later owner writes are0. If conditional publication itself is FAILED/ABSENT, do not retry. For any unresolved UNKNOWN after its one reconciliation, assign FINAL_CLASS Work immediately and perform no further GitHub writes at all. The final report records partial states. Thus no mutation crosses UNKNOWN, and no provisional A/B is ever reclassified after being final.
Use only current Rule11: fresh latest/exact-path/preimage checks; currently available GitHub functions; no required key/protocol/create_blob/CAS/direct-child/single commit/full tree/recursive fetch/unchanged-path verification; one read-only reconciliation and no resend. Head or nonconflicting owner movement alone is not STOP; latest bytes are reread/rebased. The branch-dependent identified Karen changed-path allowlist is only authority; activation/preflight receipt; scanner v2 on verified A branch; result receipt; conditional durability receipt only on known failure; snapshot; Plan; ledger; Handoff. Any other Karen path is unauthorized; mashos-api write0. A/B may be reported only after result receipt and final Handoff are fresh-verified. Every durability failure is non-outcome credit0.

# 11. Prohibited actions and counters
Specification-source materialize/stat/realpath/hash/open/read/execute; real Inspector CLI/main or production-source execution; canonical real output; technical real C01-C04/exact4; standalone target create/apply/import/execute; V3/V16/Full-R1/runtime/product/UX; app/API/DB/RN/Safety; actual-device; and mashos-api writes are all0. Existing scanner v1, Inspector V2, harness, B_R authority, receipts, and owner history are not overwritten. No diagnostic/helper source, scanner-of-scanner, expected-answer cue, case/family/proper-noun branch, fixed final text, hidden metadata, semantic omission, or generic fallback is allowed. Public records contain no raw source bodies, raw stack, secret, token, credential, private path, or environment-variable value.
All synthetic fixtures, retained technical copies, reviewer range files, stdout/stderr captures, unpublished scanner candidate, and owner candidates live only inside one fresh exact private temporary root. After final Handoff/conditional receipt verification or after the one allowed reconciliation/no-further-write decision, delete only that exact root and verify its absence. These files are not continuity owners or deliverables. Local/ZIP/bundle/chat attachment handoff is0; user-facing artifacts, execution result, and terminal record are GitHub exact paths only, except the unavoidable final chat report when GitHub is UNKNOWN/unavailable.

# 12. Required final report
The final execution report must begin, before technical terms, with exactly these six Japanese labels and concrete values:
`境界進捗は上昇したか：YES / NO`
`今回変更した実ファイル：`
`今回実行した検査・テスト：`
`今回GitHubへ残った技術成果：`
`Cycle001完了までに一つ減ったもの：`
`次に直接修正または実行するもの：`
For OUTCOME_A, the first value is YES, the technical result is the closed gate, the reduced item is one remaining boundary, and the next direct execution is exact4/standalone preparation under separate approval. For either OUTCOME_B, the first value is NO, the technical result is the uniquely proved real-file defect, the reduced item is the unresolved responsible-file uncertainty, and the next item names only the exact defective real file. For `WORK_PROCEDURE_OR_ENVIRONMENT_DEFECT`, the first value is NO, technical result is `なし`, the reduced item is `なし`, and the next item states that this path is retired and no same-series authority will be proposed; the report may separately name the one-time simpler redesign boundary without calling it progress.

# 13. Approval boundary
This proposal is not approval, activation, execution, technical result, GitHub effect, or progress. Approval authorizes only the exact paths, branch rules, final-attempt semantics, and A/B/Work classification above. OUTCOME_A does not automatically authorize boundary2. OUTCOME_B does not automatically authorize a correction. `WORK_PROCEDURE_OR_ENVIRONMENT_DEFECT` does not automatically authorize the one-time redesign. Every next technical action requires a separate Mash approval.
